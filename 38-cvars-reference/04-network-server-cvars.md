# Сеть, сервер и мультиплеер

> [⬅ Предыдущая страница](03-audio-cvars.md) | [Следующая страница ➡](05-physics-gameplay-cvars.md)

> [⬅ Вернуться к оглавлению вики](../README.md)
> [Индекс справочника cvar](../README.md#переменные-движка-cvar-reference)

Этот раздел собирает ключевые сетевые и серверные cvar движка FTEQW: публикацию сервера, лимиты клиентов, совместимость протоколов, встроенную раздачу файлов, мастер-серверы и MVD/QTV-инфраструктуру.

## Переменные

### allow_download_configs
`cvar allow_download_configs(boolean, "0")`

#### Описание и логика работы
Управляет выдачей конфигурационных файлов с сервера: расширение `.cfg` и каталог `configs/`. Значение `1` разрешает такие загрузки, но исходное описание в коде прямо помечает режим как опасный, потому что вместе с конфигами легко утекут rcon-пароли и другие приватные настройки. Это серверная переменная для администрирования раздачи контента; на публичных серверах её почти всегда держат выключенной.

#### Практические сценарии использования
```
set allow_download_configs "0" // не отдавать конфиги игрокам
```

---

### allow_download_copyrighted
`cvar allow_download_copyrighted(boolean, "0")`

#### Описание и логика работы
Разрешает или запрещает скачивание пакетов, которые движок считает потенциально защищёнными авторским правом — прежде всего архивов с префиксом `pak`. При `0` сервер не будет раздавать такие файлы встроенным download-механизмом; в описании кода отдельно рекомендовано также держать `allow_download_pakmaps 0` и `allow_download_pakcontents 0`. Это чисто серверная политика распространения ресурсов, важная для публичных инстансов и mirror/fastdl-схем.

#### Практические сценарии использования
```
set allow_download_copyrighted "0" // не раздавать спорные pak-файлы
```

---

### allow_download_demos
`cvar allow_download_demos(boolean, "1")`

#### Описание и логика работы
Разрешает загрузку файлов из каталога `demos/` обычной серверной раздачей. Значение `0` полностью отрезает доступ к демкам через встроенный download, а `1` оставляет возможность скачивать записи матчей напрямую с сервера. Полезно на закрытых лигах и тренировочных серверах, где демо должны получать только через веб-архив или вручную.

#### Практические сценарии использования
```
set allow_download_demos "1" // разрешить скачивание серверных демо
```

---

### allow_download_logs
`cvar allow_download_logs(boolean, "0")`

#### Описание и логика работы
Разрешает скачивание файлов `.log`. Встроенное описание предупреждает, что это опасно: в логах могут оказаться пароли, приватная переписка и служебная информация. Переменная серверная и в реальной эксплуатации должна оставаться `0`, кроме короткой диагностической сессии в доверенной сети.

#### Практические сценарии использования
```
set allow_download_logs "0" // не отдавать журналы сервера
```

---

### allow_download_maps
`cvar allow_download_maps(boolean, "1")`

#### Описание и логика работы
Включает раздачу файлов из каталога `maps/`. При `0` игрок без нужной карты не сможет получить её у сервера и должен будет скачать карту другим способом; при `1` сервер может закрывать пробелы в клиентском контенте автоматически. Практически это один из основных переключателей удобства подключения на кастомные мультиплеерные карты.

#### Практические сценарии использования
```
set allow_download_maps "1" // раздавать карты новым игрокам
```

---

### allow_download_models
`cvar allow_download_models(boolean, "1")`

#### Описание и логика работы
Управляет загрузкой содержимого `progs/` и `models/`. Ноль запрещает выдачу моделей и связанных progs-ресурсов через сервер, единица оставляет стандартную раздачу включённой. На мод-серверах отключение этой переменной обычно означает обязательный внешний fastdl или ручную установку пакета клиентом.

#### Практические сценарии использования
```
set allow_download_models "1" // разрешить скачивание моделей
```

---

### allow_download_pakcontents
`cvar allow_download_pakcontents(int enum, "0")`

#### Описание и логика работы
Определяет, можно ли скачивать немаповые файлы, лежащие внутри серверных пакетов. `0` полностью запрещает такую выдачу, `1` разрешает только пакеты, не помеченные как copyrighted, а `2` снимает ограничение даже для спорных архивов — код отдельно предупреждает, что это режим только ради совместимости с vanilla QuakeWorld. Переменная важна для администраторов, которые хотят балансировать между совместимостью старых клиентов и юридически безопасной раздачей контента.

#### Практические сценарии использования
```
set allow_download_pakcontents "1" // разрешить безопасную выдачу содержимого пакетов
```

---

### allow_download_pakmaps
`cvar allow_download_pakmaps(int enum, "0")`

#### Описание и логика работы
Контролирует скачивание карт, находящихся внутри пакетов сервера. `0` ничего не отдаёт, `1` разрешает выдачу только из пакетов без признаков copyrighted-контента, `2` снимает фильтр полностью, но исходный комментарий предупреждает, что использовать такой режим не следует. Это серверная переменная совместимости для старых клиентов, которые не умеют работать с пакетами так, как ожидает современный FTEQW.

#### Практические сценарии использования
```
set allow_download_pakmaps "1" // разрешить скачивание карт из безопасных пакетов
```

---

### allow_download_skins
`cvar allow_download_skins(boolean, "1")`

#### Описание и логика работы
Разрешает выдачу файлов из каталога `skins/`. При `0` игроки не смогут автоматически скачать отсутствующие скины, что уменьшает сетевую нагрузку и риск раздачи мусорных кастомизаций, но ухудшает совместимость со старыми наборами контента. Подходит для соревновательных серверов, где важнее единый визуальный стиль и минимум лишних загрузок.

#### Практические сценарии использования
```
set allow_download_skins "0" // не раздавать пользовательские скины
```

---

### allow_download_sounds
`cvar allow_download_sounds(boolean, "1")`

#### Описание и логика работы
Включает встроенную раздачу файлов из каталога `sound/`. Значение `0` полезно, если весь звуковой пакет уже лежит на fastdl/launcher-канале или если сервер сознательно не хочет раздавать модовые звуки. Значение `1` повышает шанс, что клиент догрузит нужные ресурсы без ручной подготовки.

#### Практические сценарии использования
```
set allow_download_sounds "1" // раздавать звуки клиентам
```

---

### coop
`cvar coop(boolean/int, "")`

#### Описание и логика работы
Классическая `serverinfo`-переменная режима cooperative. Пустое значение оставляет управление за конфигом или модом, а ненулевое обычно включает кооперативную логику спавна и правил. Для сетевого сервера это один из маркеров режима игры, который видят браузеры серверов и клиенты при подключении.

#### Практические сценарии использования
```
set coop "1" // запустить сервер в coop-режиме
```

---

### deathmatch
`cvar deathmatch(int enum, "1")`

#### Описание и логика работы
Основной режим сетевой игры, экспортируемый как `serverinfo`. В dedicated/server-only сборках по умолчанию стоит `1`, а допустимые значения традиционно соответствуют режимам deathmatch, задаваемым модом или игрой. Изменение напрямую влияет на правила матча и на то, как сервер идентифицируется в листингах и при рукопожатии с клиентами.

#### Практические сценарии использования
```
set deathmatch "1" // обычный deathmatch
```

---

### filterban
`cvar filterban(boolean, "1")`

#### Описание и логика работы
Определяет действие по умолчанию для IP-фильтрации и команды `addip`. При `1` фильтр трактуется как список запретов; при `0` логика инвертируется, и игроки по умолчанию кикаются, пока правило явно не разрешит доступ. Это серверная административная переменная: ошибочное переключение способно мгновенно отрезать игроков от сервера или, наоборот, открыть доступ тем, кого ожидали банить.

#### Практические сценарии использования
```
set filterban "1" // использовать список как бан-лист
```

---

### fraglimit
`cvar fraglimit(int, "")`

#### Описание и логика работы
`serverinfo`-переменная лимита фрагов на карту или раунд. Пустое значение обычно означает отсутствие лимита, а положительное число завершает матч при достижении указанного порога. На мультиплеерном сервере это одна из базовых правиловых настроек, которую клиенты видят ещё до входа на сервер.

#### Практические сценарии использования
```
set fraglimit "30" // закончить карту на 30 фрагах
```

---

### hostname
`cvar hostname(string, "unnamed")`

#### Описание и логика работы
Имя сервера, публикуемое через `serverinfo`, status-ответы и мастер-листинги. Именно эту строку игроки обычно видят в браузере серверов, поэтому сюда имеет смысл помещать короткое и читаемое название без лишнего мусора. Слишком длинные или нестандартные строки не ломают сеть сами по себе, но ухудшают сортировку, читаемость и совместимость со старыми браузерами.

#### Практические сценарии использования
```
set hostname "FTEQW Duel RU #1" // понятное имя для браузера серверов
```

---

### maxclients
`cvar maxclients(int, "8")`

#### Описание и логика работы
Ограничивает число игровых слотов для активных игроков; переменная помечена как `serverinfo` и может меняться даже посреди карты. Увеличение значения напрямую влияет на сетевую нагрузку, объём снапшотов и требования к пропускной способности; слишком высокие числа также ухудшают совместимость со старым vanilla-протоколом. Если нужно жёстко нормировать и игроков, и зрителей, переменную обычно согласуют с `sv_playerslots` и `maxspectators`.

#### Практические сценарии использования
```
set maxclients "16" // разрешить до 16 игроков
```

---

### maxspectators
`cvar maxspectators(int, "8")`

#### Описание и логика работы
Задаёт максимальное число зрителей на сервере и тоже объявлена как `serverinfo`. Значение можно менять на лету, но уменьшение лимита на переполненном сервере может привести к отказу новым наблюдателям или к вытеснению при смене карты/слотов. Полезна на турнирных серверах, где нужно отделить лимит играющих клиентов от лимита наблюдателей и QTV-инфраструктуры.

#### Практические сценарии использования
```
set maxspectators "4" // ограничить число зрителей
```

---

### net_compress
`cvar net_compress(boolean, "0")`

#### Описание и логика работы
Легаси-переключатель Huffman-сжатия сетевых пакетов. В коде это обычный флаг `0/1`, но функция относится к старой совместимости и в современной типовой конфигурации сервера её лучше не трогать без явной причины. Подробности, оговорки и контекст использования см. в [разделе про legacy/unused features](../README.md#устаревшие-экспериментальные-и-недоступные-по-умолчанию-возможности).

#### Практические сценарии использования
```
set net_compress "0" // оставить легаси-сжатие выключенным
```

---

### net_enabled
`cvar net_enabled(boolean, "1")`

#### Описание и логика работы
Глобальный рубильник сетевой подсистемы. При `0` движок перестаёт создавать сокеты и выполнять сетевое разрешение имён, хотя loopback/внутренние соединения кодом всё ещё допускаются. Для обычного сервера это почти всегда аварийная или отладочная настройка, потому что она буквально отключает внешнюю сетевую жизнь процесса.

#### Практические сценарии использования
```
set net_enabled "1" // оставить сетевой стек включённым
```

---

### net_enable_http
`cvar net_enable_http(boolean, "1")`

#### Описание и логика работы
Разрешает принимать HTTP-клиентов на TCP-портах движка. В `sv_master`-ветке код даёт этой переменной значение по умолчанию `1`, а в других HTTPSV-сборках встречается и `0`, поэтому реальный дефолт зависит от цели сборки; в любом случае смысл одинаковый — движок может обслуживать входящие HTTP-запросы и потенциально отдавать крупные файлы. Для игрового сервера это удобство, но и риск: большой HTTP-трафик способен мешать геймплейному трафику, если всё висит на одном порту и одном узком канале.

#### Практические сценарии использования
```
set net_enable_http "0" // не принимать обычные HTTP-запросы на игровом порту
```

---

### net_enable_qtv
`cvar net_enable_qtv(int enum, "2")`

#### Описание и логика работы
Управляет тем, принимает ли сервер входящие QTV-запросы от прокси и зрителей. `0` полностью запрещает такие соединения, `1` разрешает их всем, `2` ограничивает доступ локальными адресами хоста (`127.*`, `::1`, unix sockets) и потому подходит как безопасный дефолт для машин, где внешнюю публикацию берёт на себя отдельный прокси. Эта переменная важна для инфраструктуры трансляций и записи матчей, а не для обычного игрового трафика. Важно: cvar объявляется только в сборках с поддержкой `MVD_RECORDING`/QTV; без этой подсистемы её вообще не будет.

#### Практические сценарии использования
```
set net_enable_qtv "1" // разрешить внешние QTV-подключения
```

---

### net_enable_rtcbroker
`cvar net_enable_rtcbroker(boolean, "1")`

#### Описание и логика работы
Разрешает принимать websocket-подключения, используемые как брокер для прямых WebRTC/ICE-сеансов. В master-server сборке дефолт равен `1`, а в части других HTTPSV-конфигураций встречается `0`, так что при документировании нужно учитывать профиль сборки. Полезно там, где браузерные клиенты или ICE hole-punching должны находить друг друга через FTE-сервис, но лишние брокерные соединения занимают дескрипторы и создают фоновую нагрузку.

#### Практические сценарии использования
```
set net_enable_rtcbroker "0" // не принимать rtc-broker соединения на обычном матче
```

---

### net_enable_tls
`cvar net_enable_tls(boolean, "1")`

#### Описание и логика работы
Позволяет интерпретировать бинарный поток на обычном TCP-порту как TLS-handshake, то есть поднимать `https`/`wss` поверх того же порта. Это полезно для защищённых веб-клиентов, QTV и сервисных HTTP-эндпоинтов, но только если администратор действительно поддерживает TLS в своей сборке и инфраструктуре сертификатов. Для голого dedicated-сервера без веб-обвязки функция часто просто не нужна. Cvar существует только в сборках с TLS/`HAVE_SSL`; если такой поддержки нет, самой переменной не будет.

#### Практические сценарии использования
```
set net_enable_tls "1" // разрешить TLS на сетевых TCP-портах
```

---

### net_enable_websockets
`cvar net_enable_websockets(boolean, "0")`

#### Описание и логика работы
Разрешает websocket-игроков на TCP-портах движка. В `sv_master`-ветке код выставляет `0`, а в части остальных HTTPSV-сборок — `1`, поэтому фактический дефолт зависит от того, для чего собран бинарник; сама семантика не меняется. Нужна прежде всего для web-порта и браузерных клиентов, а на традиционном UDP-only сервере её можно оставить выключенной ради меньшей площади атаки.

#### Практические сценарии использования
```
set net_enable_websockets "1" // впускать websocket-клиентов
```

---

### net_hybriddualstack
`cvar net_hybriddualstack(boolean, "1")`

#### Описание и логика работы
Включает гибридные dual-stack сокеты IPv4+IPv6 там, где платформа это поддерживает. На современном хостинге это облегчает обслуживание обоих стеков с меньшим количеством отдельных биндов, но на очень старых Windows (XP и ниже, согласно комментарию) опция не работает. Для публичного сервера обычно имеет смысл оставить `1`, если нет специальных причин жёстко разнести IPv4 и IPv6.

#### Практические сценарии использования
```
set net_hybriddualstack "1" // слушать IPv4 и IPv6 гибридно
```

---

### net_ice_allowmdns
`cvar net_ice_allowmdns(boolean, "1")`

#### Описание и логика работы
Разрешает использовать [multicast](../37-quakec-builtins-reference/04-network-messages-builtins.md#multicast)-DNS для ICE-кандидатов, чтобы не раскрывать частную адресацию напрямую, а публиковать псевдонимы. При `0` такой механизм отключается, что иногда упрощает отладку, но ухудшает приватность локальной сети. Практически это настройка для WebRTC/ICE-сценариев, а не для обычного UDP-сервера.

#### Практические сценарии использования
```
set net_ice_allowmdns "1" // не отключать mDNS-кандидаты
```

---

### net_ice_allowstun
`cvar net_ice_allowstun(boolean, "1")`

#### Описание и логика работы
Управляет использованием STUN для определения публичного адреса этого узла. Если поставить `0`, движок перестанет получать server-reflexive адреса от внешних STUN-серверов, что может сорвать NAT-traversal, хотя подключение к уже присланным удалённым reflexive-кандидатам код не запрещает. Менять имеет смысл только в среде с жёсткими сетевыми политиками или при детальной диагностике ICE.

#### Практические сценарии использования
```
set net_ice_allowstun "1" // разрешить STUN-обнаружение публичного адреса
```

---

### net_ice_allowturn
`cvar net_ice_allowturn(boolean, "1")`

#### Описание и логика работы
Определяет, может ли движок регистрировать TURN-соединения и использовать релейные кандидаты. Значение `0` отключает попытки получить собственный relay-маршрут, но не мешает подключаться к relay-кандидатам другой стороны. На сервере, который должен пробиваться через сложный NAT или работать с браузерными клиентами, отключение TURN обычно резко снижает шанс успешного соединения.

#### Практические сценарии использования
```
set net_ice_allowturn "1" // разрешить TURN-релей для ICE
```

---

### net_ice_broker
`cvar net_ice_broker(string URL, "tls://master.frag-net.com:27950")`

#### Описание и логика работы
Адрес брокера, через который FTE пытается организовывать ICE/RTC-соединения для `sv_public /foo` и `connect /foo`. По сути это координатор hole-punching и обмена кандидатами, поэтому неправильный URL делает красивые slash-адреса недоступными, даже если сам игровой сервер жив. Переменная особенно полезна для собственных мастер/брокер-инсталляций и закрытых экосистем.

#### Практические сценарии использования
```
set net_ice_broker "tls://master.frag-net.com:27950" // использовать стандартный брокер FTE
```

---

### net_ice_relayonly
`cvar net_ice_relayonly(boolean, "0")`

#### Описание и логика работы
Если включить `1`, движок перестанет рекламировать нерелейные локальные кандидаты и будет пытаться ходить к удалённой стороне только через relay. Такой режим повышает предсказуемость за жёстким NAT, но добавляет лишнюю задержку, трафик и зависимость от TURN/relay-сервиса. На обычном сервере без специфических WebRTC-задач лучше оставлять `0`.

#### Практические сценарии использования
```
set net_ice_relayonly "0" // разрешить прямые ICE-маршруты
```

---

### net_ice_servers
`cvar net_ice_servers(string list, "")`

#### Описание и логика работы
Список ICE-серверов через пробел: STUN и TURN адреса вида `stun:host:port` или `turn:host:port?...`. Пустое значение означает, что движок полагается на встроенные значения/внешний брокер и не получает явный пользовательский набор серверов. Переменная полезна для собственных WebRTC-развёртываний, где администратор хочет контролировать, через какие STUN/TURN-узлы будут строиться соединения.

#### Практические сценарии использования
```
set net_ice_servers "stun:stun.l.google.com:19302" // добавить внешний STUN-сервер
```

---

### net_mtu
`cvar net_mtu(int, "1440")`

#### Описание и логика работы
Ограничивает максимальный размер UDP-payload до фрагментации. В web-сборках значение по умолчанию `1384`, в обычной — `1440`; смысл один и тот же: если маршрут плохо переносит IP-фрагментацию, значение уменьшают, чтобы большие пакеты не разваливались по дороге.

#### Практические сценарии использования
```
set net_mtu "1200" // уменьшить MTU для сложного NAT/туннеля
```

---

### password
`cvar password(string, "")`

#### Описание и логика работы
Пароль на вход в игру. Пустая строка означает открытый доступ, непустая — что клиенту нужно знать пароль ещё до полноценного входа на сервер. Это базовая серверная защита приватных матчей; на публичном сервере случайное заполнение переменной фактически превратит его в закрытый.

#### Практические сценарии использования
```
set password "clanmatch" // закрыть сервер паролем
```

---

### qtv_maxstreams
`cvar qtv_maxstreams(int or empty, "0")`

#### Описание и логика работы
Лимит прямых QTV-клиентов и прокси, которые могут одновременно висеть на сервере. По описанию кода пустое значение снимает ограничение, `0` полностью запрещает стриминг, а положительные числа задают жёсткий потолок. Это уже не лимит игровых клиентов, а именно лимит трансляционной нагрузки, поэтому на турнирных серверах его обычно настраивают отдельно от `maxspectators`. Практический смысл cvar есть только в сборках с MVD/QTV-подсистемой; без неё переменная отсутствует.

#### Практические сценарии использования
```
set qtv_maxstreams "32" // разрешить до 32 QTV-подключений
```

---

### rcon_password
`cvar rcon_password(string, "")`

#### Описание и логика работы
Пароль удалённого администрирования ([`rcon`](../44-cli-commands-reference/02-client-ui-commands.md#rcon)). Пустое значение фактически лишает смысла внешние rcon-команды, а непустое открывает полный удалённый контроль над сервером тем, кто знает секрет. На публичном хосте эту переменную обязательно сочетают с `sv_crypt_rcon`, `sv_rconlim` и аккуратным обращением с логами/конфигами.

#### Практические сценарии использования
```
set rcon_password "change_me_now" // включить защищённый rcon-доступ
```

---

### spectator_password
`cvar spectator_password(string, "")`

#### Описание и логика работы
Отдельный пароль для входа зрителем. Позволяет держать матч закрытым для игроков, но при этом впускать судей, комментаторов или организаторов по другому секрету. Пустая строка оставляет слот зрителя открытым, поэтому на кланварных серверах эту переменную обычно задают явно.

#### Практические сценарии использования
```
set spectator_password "speconly" // дать отдельный пароль для наблюдателей
```

---

### sv_banproxies
`cvar sv_banproxies(boolean, "0")`

#### Описание и логика работы
Если включить, сервер будет отказывать клиентам, распознанным как известные proxy-программы. Встроенное описание честно предупреждает, что метод полезен против части аимботных/прокси-связок, но надёжен далеко не для всех публичных прокси. Это скорее фильтр риска, чем гарантия безопасности: он может отсечь нежелательных посредников, но не заменяет полноценную античит-политику.

#### Практические сценарии использования
```
set sv_banproxies "1" // блокировать известных proxy-клиентов
```

---

### sv_bigcoords
`cvar sv_bigcoords(boolean, "1")`

#### Описание и логика работы
Переключает сетевое кодирование координат на float вместо 16-битного формата и одновременно повышает точность углов. Это полезно не только для больших карт, но и для модов, которым важна геометрическая точность; однако цена — обязательное использование расширений протокола или альтернативных протокольных режимов (`QW` extension, `DPP7`, `RMQ 999`). Если на сервер заходят старые клиенты без нужной поддержки, их подключение или воспроизведение может сломаться.

#### Практические сценарии использования
```
set sv_bigcoords "1" // включить расширенные координаты
```

---

### sv_calcphs
`cvar sv_calcphs(int enum, "2")`

#### Описание и логика работы
Управляет расчётом PHS для отсечения звуковых событий. `0` отключает PHS и делает звуки глобальными, `1` всегда строит PHS, а `2` пропускает генерацию на больших single-player картах, сохраняя поведение `1` в остальных случаях. Это серверная настройка сетевой оптимизации: слишком консервативные значения тратят лишний трафик, а слишком агрессивные могут менять слышимость событий на крупных картах.

#### Практические сценарии использования
```
set sv_calcphs "2" // использовать адаптивный расчёт PHS
```

---

### sv_crypt_rcon
`cvar sv_crypt_rcon(string/switch, "")`

#### Описание и логика работы
Определяет, должен ли rcon-пароль передаваться в хешированном виде. `0` принудительно оставляет только голый plaintext и прямо помечен как небезопасный, `1` требует хеширование и является рекомендуемым режимом, а пустое значение сохраняет совместимость со старыми клиентами, позволяя оба варианта. Важно понимать ограничение, указанное в коде: даже хешированный режим снижает риск replay-атак, но не делает rcon-поток конфиденциальным сам по себе.

#### Практические сценарии использования
```
set sv_crypt_rcon "1" // требовать хешированный rcon
```

---

### sv_cullplayers_trace
`cvar sv_cullplayers_trace(boolean/int, "")`

#### Описание и логика работы
Пытается отсеивать сетевую репликацию игроков трассировками как анти-wallhack меру. Это серверная `serverinfo`-настройка: клиенты узнают о политике сервера, а сам сервер тратит дополнительные вычисления на проверку видимости. Полезна на соревновательных публичных серверах, но излишне агрессивное использование может привести к спорным пропаданиям целей на нестандартных картах.

#### Практические сценарии использования
```
set sv_cullplayers_trace "1" // включить trace-based culling игроков
```

---

### sv_demoClearOld
`cvar sv_demoClearOld(boolean, "0")`

#### Описание и логика работы
Разрешает автоматически удалять старые MVD-демо, чтобы каталог не разрастался бесконтрольно. Сам по себе флаг не задаёт возраст или количество — он работает совместно с ограничителями `sv_demoMaxDirAge`, `sv_demoMaxDirCount` и `sv_demoMaxDirSize`. На сервере с автозаписью это один из ключевых переключателей обслуживания диска.

#### Практические сценарии использования
```
set sv_demoClearOld "1" // автоматически чистить старые MVD
```

---

### sv_demoExtensions
`cvar sv_demoExtensions(int enum, "1")`

#### Описание и логика работы
Разрешает дополнительные протокольные расширения внутри записываемых MVD. `0` выключает их, `1` включает весь набор расширений FTE, `2` ограничивает запись тем, что понимает ещё один совместимый движок. Чем выше совместимость со старыми плеерами, тем меньше возможностей демо; чем больше расширений, тем богаче запись, но тем выше риск, что старый клиент откажется её воспроизводить.

#### Практические сценарии использования
```
set sv_demoExtensions "1" // писать MVD с расширениями FTE
```

---

### sv_demofps
`cvar sv_demofps(int, "30")`

#### Описание и логика работы
Частота фиксации данных в MVD-записи. Более высокие значения делают демо плавнее и точнее по времени, но увеличивают объём файлов и интенсивность записи; более низкие экономят место ценой грубого тайминга. Для обычных матчей дефолт `30` — разумный компромисс.

#### Практические сценарии использования
```
set sv_demofps "30" // оставить стандартную частоту записи MVD
```

---

### sv_demoMaxDirAge
`cvar sv_demoMaxDirAge(time/int, "0")`

#### Описание и логика работы
Максимальный возраст MVD-файлов, используемый очисткой при активном `sv_demoClearOld`. `0` по умолчанию не ограничивает возраст, а положительное значение заставляет сборщик удалять слишком старые записи, не мешая при этом созданию новых демо само по себе. Это настройка гигиены хранилища, особенно полезная на серверах с автозаписью каждого матча.

#### Практические сценарии использования
```
set sv_demoMaxDirAge "2592000" // хранить демо не дольше 30 дней
```

---

### sv_demoMaxDirCount
`cvar sv_demoMaxDirCount(int, "500")`

#### Описание и логика работы
Лимит количества записанных сервером MVD-файлов. `0` отключает ограничение, любое положительное число ставит потолок, после которого новые демо больше не начнут запись, пока старые не будут удалены или очищены автоматически. Полезен для предсказуемого расхода inode/каталогов там, где место на диске ограничено.

#### Практические сценарии использования
```
set sv_demoMaxDirCount "1000" // разрешить хранить до 1000 MVD
```

---

### sv_demoMaxDirSize
`cvar sv_demoMaxDirSize(size/string, "100mb")`

#### Описание и логика работы
Максимальный суммарный объём диска, который может занимать каталог серверных MVD. Пустая строка снимает лимит, а значения с суффиксами вроде `mb`/`gb` позволяют жёстко ограничить архив. Когда предел достигнут, новые демо уже не стартуют, поэтому эта переменная напрямую влияет на надёжность автозаписи длительных сезонов.

#### Практические сценарии использования
```
set sv_demoMaxDirSize "2gb" // ограничить архив MVD двумя гигабайтами
```

---

### sv_demoMaxSize
`cvar sv_demoMaxSize(size/string, "")`

#### Описание и логика работы
Обрезает каждое отдельное демо до указанного максимального размера. Пустое значение не накладывает потолка, а заполненное защищает от слишком длинных записей, которые разрастаются на многочасовых матчах или виснущих серверах. Практически это предохранитель против единичных «монструозных» MVD, а не общий лимит архива.

#### Практические сценарии использования
```
set sv_demoMaxSize "500mb" // не позволять одному демо вырасти слишком сильно
```

---

### sv_demoUseCache
`cvar sv_demoUseCache(boolean/int, "")`

#### Описание и логика работы
Если переменная включена, сервер буферизует запись MVD и сбрасывает данные на диск периодически, а не после каждого малого фрагмента. Это уменьшает количество мелких операций ввода-вывода и может помочь HDD/сетевым файловым системам, но повышает риск потерять хвост записи при жёстком падении процесса. Хороший вариант для активных публичных серверов, где важна производительность дисковой подсистемы.

#### Практические сценарии использования
```
set sv_demoUseCache "1" // буферизовать запись MVD на диск
```

---

### sv_demo_write_csqc
`cvar sv_demo_write_csqc(boolean/int, "")`

#### Описание и логика работы
Вкладывает копию `csprogs` в записываемое демо. Это делает файл тяжелее, но защищает воспроизведение от будущих изменений геймкода: демо остаётся самодостаточным даже после апдейта мода. Практически полезно для долгосрочного архива матчей и для модов, где CSQC меняется часто.

#### Практические сценарии использования
```
set sv_demo_write_csqc "1" // вкладывать csprogs в демо
```

---

### sv_guidkey
`cvar sv_guidkey(string, "")`

#### Описание и логика работы
Меняет базу, от которой клиент вычисляет свой GUID: вместо серверного IP используется заданная строка. Это удобно, если нескольким серверам нужен единый стабильный идентификатор игроков для статистики, но код отдельно предупреждает, что такой GUID нельзя считать чем-то криптографически надёжным. Пустое значение оставляет стандартное поведение, завязанное на адрес сервера.

#### Практические сценарии использования
```
set sv_guidkey "shared-guid-salt" // сделать GUID единым для кластера серверов
```

---

### sv_heartbeat_checks
`cvar sv_heartbeat_checks(boolean, "1")`

#### Описание и логика работы
Разрешает диагностические сообщения, когда `sv_public 1` не срабатывает из-за вероятных проблем с NAT/роутером. Это не меняет сам протокол heartbeat, а лишь помогает администратору понять, почему сервер не виден снаружи. На публичных серверах полезно держать включённым, особенно при первом запуске за новым NAT.

#### Практические сценарии использования
```
set sv_heartbeat_checks "1" // сообщать о проблемах публикации сервера
```

---

### sv_heartbeat_interval
`cvar sv_heartbeat_interval(int, "110")`

#### Описание и логика работы
Интервал отправки heartbeat на мастер-серверы. Код прямо предупреждает, что слишком маленькие значения выглядят как злоупотребление, а слишком большие способны вызывать ghost/NAT-проблемы и выпадение из листинга. Для публичного сервера обычно лучше держаться около дефолта, а не пытаться «ускорить» публикацию частыми пингами мастера.

#### Практические сценарии использования
```
set sv_heartbeat_interval "110" // оставить умеренный интервал heartbeat
```

---

### sv_heartbeattimeout
`cvar sv_heartbeattimeout(int, "300")`

#### Описание и логика работы
Сколько секунд мастер-сервер держит запись о сервере после последнего heartbeat.

#### Практические сценарии использования
```
set sv_heartbeattimeout "300" // держать запись о сервере 5 минут после последнего heartbeat
```

---

### sv_limittics
`cvar sv_limittics(int, "3")`

#### Описание и логика работы
Лимит количества физико-сетевых тиков, которые сервер может прогнать за один кадр, чтобы нагнать отставание. Более высокое число помогает быстрее восстанавливаться после коротких фризов, но может вызывать тяжёлые burst-кадры и резкие всплески CPU; слишком низкое — оставляет симуляцию «вязнуть» в долге. Настройка связана с `sv_maxtic`: если окно между тиками мало, именно `sv_limittics` ограничивает степень догоняния.

#### Практические сценарии использования
```
set sv_limittics "3" // не выполнять слишком много тик-итераций за кадр
```

---

### sv_listen_dp
`cvar sv_listen_dp(boolean, "0")`

#### Описание и логика работы
Легаси-переключатель совместимости для старого DP-специфичного handshake. В обычной современной конфигурации его лучше держать выключенным; если вам действительно нужна поддержка таких старых путей подключения, подробности и риски собраны в [разделе про legacy/unused features](../README.md#устаревшие-экспериментальные-и-недоступные-по-умолчанию-возможности).

#### Практические сценарии использования
```
set sv_listen_dp "0" // не включать старый DP-handshake без необходимости
```

---

### sv_listen_nq
`cvar sv_listen_nq(int enum, "0")`

#### Описание и логика работы
Легаси-переключатель допуска NetQuake-клиентов. `0` оставляет старый NQ-вход закрытым; режимы `1` и `2` нужны только ради совместимости со старым клиентским зоопарком и имеют свои оговорки по безопасности и поведению. Подробности — в [разделе про legacy/unused features](../README.md#устаревшие-экспериментальные-и-недоступные-по-умолчанию-возможности).

#### Практические сценарии использования
```
set sv_listen_nq "0" // не впускать старые NQ-клиенты без явной необходимости
```

---

### sv_maxdrate
`cvar sv_maxdrate(speed/int, "500000")`

#### Описание и логика работы
Ограничивает скорость, с которой сервер отдаёт данные одному клиенту во время скачивания файлов. `0` снимает серверный предел вообще, что вместе с пустым/нулевым клиентским `drate` может привести к очень агрессивной раздаче и фактически к DoS-подобной нагрузке на канал. На публичных серверах это один из главных балансировочных параметров между удобством fast-download и сохранением нормального пинга у играющих.

#### Практические сценарии использования
```
set sv_maxdrate "250000" // урезать скорость скачивания, чтобы не душить матч
```

---

### sv_maxgames
`cvar sv_maxgames(int, "100")`

#### Описание и логика работы
Переменная master-server процесса: максимум известных игр/гейм-идентификаторов, которые он хранит. Ограничение введено именно как анти-DoS мера, чтобы злоумышленник не засыпал мастер бесконечным количеством фиктивных «игр». Для обычного игрового сервера эта cvar неактуальна; она нужна операторам собственных каталогов серверов.

#### Практические сценарии использования
```
set sv_maxgames "200" // разрешить мастеру обслуживать больше разных игр
```

---

### sv_maxrate
`cvar sv_maxrate(speed/int, "50000")`

#### Описание и логика работы
Верхний предел байт в секунду, который сервер готов отправлять одному игроку вне режима скачивания. Реальная скорость игрока дополнительно ограничивается его собственным `rate`, поэтому `sv_maxrate` — это именно серверный потолок. Слишком низкое значение вызывает дёрганый апдейт мира и потерю плавности, слишком высокое может ухудшить жизнь клиентам с плохим интернетом и быстрее забить исходящий канал сервера.

#### Практические сценарии использования
```
set sv_maxrate "75000" // слегка поднять потолок игрового трафика
```

---

### sv_maxservers
`cvar sv_maxservers(int, "10000")`

#### Описание и логика работы
Общий максимум серверов, известных мастер-серверу по всем играм сразу.

#### Практические сценарии использования
```
set sv_maxservers "20000" // увеличить общий лимит серверов на мастере
```

---

### sv_maxtic
`cvar sv_maxtic(float, "0.1")`

#### Описание и логика работы
Максимальный интервал между серверными физическими тиками. Слишком маленькое значение заставляет сервер чаще догонять отставание несколькими тик-итерациями подряд, а `0` переводит поведение в фиксированный интервал, что может требоваться для ODE-сценариев. Это одна из фундаментальных настроек темпа симуляции: она влияет и на CPU, и на латентность реакции сервера на накопившееся время.

#### Практические сценарии использования
```
set sv_maxtic "0.05" // делать серверные тики чаще
```

---

### sv_minping
`cvar sv_minping(int, "")`

#### Описание и логика работы
Искусственно добавляет задержку игрокам, у которых реальный ping ниже заданного порога. Значение задаётся в миллисекундах и используется как «уравнитель» на некоторых серверах, где хотят сгладить преимущество локальных игроков. Это спорный инструмент: он действительно меняет сетевое ощущение игры и ухудшает отзывчивость, поэтому применять его стоит только осознанно.

#### Практические сценарии использования
```
set sv_minping "30" // добавить минимум 30 мс самым близким игрокам
```

---

### sv_mintic
`cvar sv_mintic(float, "0.013")`

#### Описание и логика работы
Минимальный интервал между физическими тиками сервера. В dedicated/server-only варианте значение по умолчанию `0.013`, а в некоторых клиентских сборках встречается `0`, чтобы локальная сторона могла думать без ограничения; для серверной документации основной ориентир — именно `0.013`.

#### Практические сценарии использования
```
set sv_mintic "0.013" // оставить стандартную минимальную длительность тика
```

---

### sv_nailhack
`cvar sv_nailhack(boolean, "1")`

#### Описание и логика работы
Переключает историческую сетевую оптимизацию nail-энтити. При `1`, согласно описанию, оптимизация отключается — это совместимо с привычками Qizmo, улучшает интерполяцию полёта гвоздей у клиентов и позволяет рисовать трейлы, но меняет сетевое представление этих объектов. Переменная полезна прежде всего для QuakeWorld-совместимых сценариев и старых модов, где nail-projectiles активно используются.

#### Практические сценарии использования
```
set sv_nailhack "1" // использовать совместимый режим передачи nail-снарядов
```

---

### sv_playerslots
`cvar sv_playerslots(int or empty, "")`

#### Описание и логика работы
Общий лимит слотов игроков, зрителей и ботов; новое значение применяется со следующей карты и может привести к кикам, а alias `maxplayers` используется для совместимости. Рекомендуется держать число примерно равным `maxclients + maxspectators`.

#### Практические сценарии использования
```
set sv_playerslots "24" // зарезервировать 24 общих слота на сервере
```

---

### sv_protocol
`cvar sv_protocol(string list, "")`

#### Описание и логика работы
Форсирует конкретные расширения сетевого протокола: `fte1`, `fte2`, `csqc`.

#### Практические сценарии использования
```
set sv_protocol "fte1 fte2" // требовать базовые расширения FTE
```

---

### sv_public
`cvar sv_public(int enum, "0")`

#### Описание и логика работы
Главный переключатель публичности сервера. `-1` полностью блокирует входящие подключения, `0` оставляет сервер частным/LAN-only без подписки на мастер, `1` публикует адрес на публичных master-серверах; в ICE/Web-сборках есть и режим `2`, где сервер публикуется через broker для hole-punching. Ошибочная настройка здесь — одна из самых частых причин, почему сервер либо внезапно исчезает из браузера, либо наоборот светится в интернете, когда этого не хотели.

#### Практические сценарии использования
```
set sv_public "1" // опубликовать сервер на мастер-серверах
```

---

### sv_rconlim
`cvar sv_rconlim(int, "4")`

#### Описание и логика работы
Ограничивает повторные невалидные попытки `rcon`. Небольшие значения уменьшают окно для перебора пароля, но могут случайно задеть администратора, который несколько раз ошибся подряд; слишком мягкие значения почти не мешают brute-force. Это одна из обязательных защит публичного dedicated-сервера, если `rcon_password` вообще используется.

#### Практические сценарии использования
```
set sv_rconlim "4" // оставить умеренный лимит на неверный rcon
```

---

### sv_reconnectlimit
`cvar sv_reconnectlimit(int, "0")`

#### Описание и логика работы
Не даёт одному и тому же адресу/порту мгновенно переподключаться в течение заданного времени. Ноль отключает ограничение, положительное значение защищает от части спама переподключениями и помогает не переиспользовать ещё «зомбифицированный» слот слишком рано. Слишком большой интервал раздражает игроков с нестабильной связью, слишком маленький почти не защищает от дёрганья connect/disconnect.

#### Практические сценарии использования
```
set sv_reconnectlimit "3" // запретить мгновенные повторные входы
```

---

### sv_reliable_sound
`cvar sv_reliable_sound(boolean, "0")`

#### Описание и логика работы
Переводит отправку звуков в надёжный канал, чтобы packet loss не съедал важные аудио-события. Цена — задержка и более «пачечная» доставка, то есть звук приходит надёжнее, но не так своевременно; кроме того, отдельные клиенты могут переопределять поведение через userinfo `rsnd`. Полезно на сервере с действительно плохими каналами или критичными звуковыми сигналами, но не всегда хорошо ощущается в динамичном дуэльном геймплее.

#### Практические сценарии использования
```
set sv_reliable_sound "1" // посылать звуки надёжно
```

---

### sv_reportheartbeats
`cvar sv_reportheartbeats(int enum, "2")`

#### Описание и логика работы
Управляет тем, насколько многословно сервер сообщает об отправке heartbeat на мастер. При `1` уведомления идут каждый раз, при `2` сообщение показывается один раз, а `0` фактически делает процесс тихим. Это диагностическая, а не протокольная переменная: полезна, когда вы настраиваете публикацию сервера и хотите убедиться, что heartbeat действительно уходит.

#### Практические сценарии использования
```
set sv_reportheartbeats "1" // печатать heartbeat-сообщения в консоль
```

---

### sv_serverip
`cvar sv_serverip(string address, "")`

#### Описание и логика работы
Ручной внешний адрес сервера, который движок должен объявлять, если сам не может надёжно определить публичный IP за NAT/фаерволом. Если NAT ещё и ремапит порт, его тоже нужно указать в строке. Это один из самых практичных параметров при хостинге из домашней сети: без него мастер-сервер может раздавать клиентам бесполезный внутренний адрес.

#### Практические сценарии использования
```
set sv_serverip "203.0.113.10:27500" // сообщать наружу правильный публичный адрес
```

---

### sv_slaverequery
`cvar sv_slaverequery(int, "120")`

#### Описание и логика работы
Переменная master-сервера: как часто он заново опрашивает slave-master узлы. Большие значения уменьшают фоновый трафик между каталогами, но обновляют агрегированные списки медленнее; слишком маленькие добавляют лишнюю сетевую болтовню. Нужна только тем, кто эксплуатирует распределённую систему мастер-серверов на базе FTE.

#### Практические сценарии использования
```
set sv_slaverequery "120" // переопрашивать slave-master раз в 120 секунд
```

---

### sv_timestamplen
`cvar sv_timestamplen(int, "60")`

#### Описание и логика работы
Хотя внутренняя переменная называется `sv_crypt_rcon_clockskew`, наружу экспортируется именно `sv_timestamplen`: она ограничивает допустимый сдвиг часов для снижения delayed replay-атак на hashed rcon. Меньшее окно усиливает защиту, но повышает шанс отказа при рассинхронизации времени у клиента-админа; большее — удобнее, но мягче к повтору захваченных пакетов. Работает в паре с `sv_crypt_rcon`.

#### Практические сценарии использования
```
set sv_timestamplen "30" // сузить окно допустимого clock skew для rcon
```

---

### sv_use_dns
`cvar sv_use_dns(boolean/string, "")`

#### Описание и логика работы
Включает reverse-DNS lookup, чтобы сервер мог показывать более читаемую информацию о том, откуда подключаются клиенты. Полезно для логов, администрирования и некоторых бан-политик, но добавляет стоимость DNS-разрешения и иногда задерживает диагностические выводы. На чисто производительном публичном сервере часто оставляют выключенным, а на админских/комьюнити серверах — включают ради удобства.

#### Практические сценарии использования
```
set sv_use_dns "1" // показывать хостнеймы клиентов в логах
```

---

### teamplay
`cvar teamplay(int, "")`

#### Описание и логика работы
Классическая `serverinfo`-переменная командного режима. Пустое значение оставляет поведение за модом или конфигом, ненулевые значения обычно выбирают конкретный вариант teamplay-правил. Для сетевого окружения она важна не только логикой матча, но и тем, как сервер помечается в браузерах и MVD-автоименовании.

#### Практические сценарии использования
```
set teamplay "1" // включить командный режим
```

---

### timelimit
`cvar timelimit(int, "")`

#### Описание и логика работы
Лимит времени на карту/раунд, экспортируемый через `serverinfo`. Пустая строка обычно означает бесконечную карту, а положительное число завершает матч по таймеру. Это одна из базовых сетевых настроек публичных ротаций и турнирных конфигов.

#### Практические сценарии использования
```
set timelimit "20" // ограничить карту двадцатью минутами
```

---

### timeout
`cvar timeout(int, "65")`

#### Описание и логика работы
Глобальный сетевой таймаут: если от соединения не приходит ни одного пакета дольше указанного числа секунд, оно считается умершим. Слишком низкое значение агрессивно выбрасывает клиентов с плавающим интернетом, слишком высокое — заставляет сервер дольше держать мёртвые подключения и слоты. На практике именно с `timeout` связана грань между «терпимостью к лагам» и скоростью очистки зависших клиентов.

#### Практические сценарии использования
```
set timeout "65" // держать стандартный таймаут соединений
```

---

### zombietime
`cvar zombietime(int, "2")`

#### Описание и логика работы
Сколько секунд сервер не переиспользует клиентский слот после разрыва, пока «досасывает» поздние пакеты и хвосты состояния. Малое значение ускоряет освобождение мест, но повышает риск коллизий при мгновенном переподключении; большое — делает сервер более аккуратным к сетевому мусору, но медленнее возвращает слоты в пул. Работает вместе с `sv_reconnectlimit`, хотя решает чуть другую задачу.

#### Практические сценарии использования
```
set zombietime "2" // оставить короткое зомби-окно для слота
```

---

### allow_download
`cvar allow_download(int, "1")`

#### Описание и логика работы
Если 1, загрузка разрешена. Установите значение 0, чтобы безоговорочно заблокировать *ВСЕ* загрузки с этого сервера. Вы можете установить sv_dlURL, если хотите, чтобы клиенты по-прежнему могли загружать контент.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется [`vid_restart`](../44-cli-commands-reference/02-client-ui-commands.md#vid_restart) или [`vid_reload`](../44-cli-commands-reference/02-client-ui-commands.md#vid_reload).

#### Практическое использование
```cfg
seta allow_download "1" // вернуть/установить значение по умолчанию
```

---

### allow_download_locs
`cvar allow_download_locs(int, "1")`

#### Описание и логика работы
0 блокирует загрузку любого файла в каталоге locs/.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta allow_download_locs "1" // вернуть/установить значение по умолчанию
```

---

### allow_download_other
`cvar allow_download_other(int, "0")`

#### Описание и логика работы
0 блокирует загрузку любого файла, который не был включен ни в один из блоков загрузки каталога.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta allow_download_other "0" // вернуть/установить значение по умолчанию
```

---

### allow_download_packages
`cvar allow_download_packages(int, "1")`

#### Описание и логика работы
Если 1, разрешается загрузка файлов (из корневого каталога или другого места) с известными расширениями пакетов (например: pak+pk3). На пакеты с именем, начинающимся с «pak», также распространяется действиеallow_download_copyrighted.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta allow_download_packages "1" // вернуть/установить значение по умолчанию
```

---

### allow_download_particles
`cvar allow_download_particles(int, "1")`

#### Описание и логика работы
0 блокирует загрузку любого файла в каталоге частиц/.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta allow_download_particles "1" // вернуть/установить значение по умолчанию
```

---

### allow_download_refpackages
`cvar allow_download_refpackages(int, "1")`

#### Описание и логика работы
Если установлено значение 1, пакеты, содержащие файлы, необходимые во время функций создания, будут становиться «ссылками» и автоматически загружаться клиентам.  
Этот параметр, вероятно, не следует устанавливать, если у вас есть большие пакеты, которые предоставляют модели получения замены на общедоступных серверах.  
Команда пути отобразит тег «(ref)» рядом с пакетами, которые клиенты будут автоматически пытаться загрузить.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta allow_download_refpackages "1" // вернуть/установить значение по умолчанию
```

---

### allow_download_root
`cvar allow_download_root(int, "0")`

#### Описание и логика работы
Если установлено, разрешает загрузку из корня игрового каталога (а не из базового). Этот параметр имеет более низкий приоритет, чем проверки на основе расширений.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta allow_download_root "0" // вернуть/установить значение по умолчанию
```

---

### allow_download_textures
`cvar allow_download_textures(int, "1")`

#### Описание и логика работы
0 блокирует загрузку любого файла в директорииtextures/

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta allow_download_textures "1" // вернуть/установить значение по умолчанию
```

---

### allow_download_wads
`cvar allow_download_wads(int, "1")`

#### Описание и логика работы
0 блокирует загрузку любого файла, находящегося в каталоге wads/ или находящегося в корневом каталоге с расширением .[wad](../39-entity-keys-reference/01-worldspawn-common-keys.md#wad).

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta allow_download_wads "1" // вернуть/установить значение по умолчанию
```

---

### capturerate
`cvar capturerate(int, "30")`

#### Описание и логика работы
Частота кадров записываемого видео

Значение по умолчанию — `"30"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta capturerate "30" // вернуть/установить значение по умолчанию
```

---

### cl_download_csprogs
`cvar cl_download_csprogs(int, "1")`

#### Описание и логика работы
Загрузите обновленный игровой код клиента, если он доступен. Предупреждение. Если вы очистите этот параметр, чтобы избежать загрузки кода виртуальной машины, вам также следует очистить cl_download_packages.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta cl_download_csprogs "1" // вернуть/установить значение по умолчанию
```

---

### cl_download_mapsrc
`cvar cl_download_mapsrc(string, "")`

#### Описание и логика работы
Указывает префикс местоположения http для загрузки карт. НАПРИМЕР: \"http://example.com/path/gamemaps/\"

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta cl_download_mapsrc "" // вернуть/установить значение по умолчанию
```

---

### cl_download_packages
`cvar cl_download_packages(int, "1")`

#### Описание и логика работы
0=Не загружать пакеты просто потому, что их использует сервер. 1=Скачивайте и загружайте пакеты по мере необходимости (не влияет на игры, которые не используют этот пакет). 2=Загрузить и установить навсегда (используйте с осторожностью!)

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta cl_download_packages "1" // вернуть/установить значение по умолчанию
```

---

### cl_download_redirection
`cvar cl_download_redirection(int, "2")`

#### Описание и логика работы
Следуйте перенаправлению загрузки, чтобы загружать пакеты вместо отдельных файлов. Также позволяет серверу отправлять практически произвольные команды загрузки.

- **2:** позволяет перенаправление только на файлы именованных пакетов (и demos/*.mvd), что немного безопаснее.

Значение по умолчанию — `"2"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta cl_download_redirection "2" // вернуть/установить значение по умолчанию
```

---

### cl_download_wait
`cvar cl_download_wait(int, "1")`

#### Описание и логика работы
0=присоединиться к игре еще до завершения загрузки (может быть с задержкой). 1=дождитесь завершения всех загрузок, прежде чем присоединяться.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta cl_download_wait "1" // вернуть/установить значение по умолчанию
```

---

### cl_downloads
`cvar cl_downloads(int, "1")`

#### Описание и логика работы
Позволяет блокировать все автоматические загрузки.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta cl_downloads "1" // вернуть/установить значение по умолчанию
```

---

### com_fullgamename
`cvar com_fullgamename(string, "fs_gamename")`

#### Описание и логика работы
Файловая система пытается запустить эту игру.

Значение по умолчанию — `"fs_gamename"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta com_fullgamename "fs_gamename" // вернуть/установить значение по умолчанию
```

---

### com_gamedirnativecode
`cvar com_gamedirnativecode(int, "0")`

#### Описание и логика работы
), который позже запускается из того же игрового каталога.  
Quake2, Quake3 (при отладке) и KTX обычно запускают собственный игровой код из игровых каталогов, поэтому, если вы хотите запустить любую из этих игр, вам необходимо убедиться, что эта переменная изменена на 1, а также убедиться, что вы не запускаете небезопасные клиенты.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta com_gamedirnativecode "0" // вернуть/установить значение по умолчанию
```

---

### com_highlightcolor
`cvar com_highlightcolor(string, "ANSI colour to be used for highlighted text, used when com_parseutf8 is active.")`

#### Описание и логика работы
Задаёт ANSI-код цвета для подсвеченного текста в консольном выводе. По объявлению он связан с режимом com_parseutf8 и предназначен именно для визуального выделения текста, а не для игровой логики. Прямые обращения к нему в C-коде встречаются редко, поэтому это в первую очередь настройка оформления консоли.


#### Практическое использование
```cfg
seta com_highlightcolor "ANSI colour to be used for highlighted text, used when com_parseutf8 is active." // вернуть/установить значение по умолчанию
```

---

### com_parseutf8
`cvar com_parseutf8(int, "1")`

#### Описание и логика работы
Интерпретируйте консольные сообщения/имена игроков/и т. д. как UTF-8. Требуются специальные шрифты. -1=изо 8859-1. 0=quakeascii (в чате используются большие символы). 1=utf8, возврат к ascii при ошибках декодирования. 2=utf8 игнорирует ошибки

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta com_parseutf8 "1" // вернуть/установить значение по умолчанию
```

---

### com_protocolname
`cvar com_protocolname(string, "com_gamename")`

#### Описание и логика работы
Имя протокола игры, используемое для запросов dpmaster. Для совместимости с DP вы можете установить для этого параметра значение «DarkPlaces-Quake», чтобы он был указан на главном сервере DP и отображал список серверов DP.

Значение по умолчанию — `"com_gamename"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta com_protocolname "com_gamename" // вернуть/установить значение по умолчанию
```

---

### com_protocolversion
`cvar com_protocolversion(int, "3")`

#### Описание и логика работы
Версия протокола, используемая для запросов dpmaster.

Значение по умолчанию — `"3"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta com_protocolversion "3" // вернуть/установить значение по умолчанию
```

---

### drate
`cvar drate(int, "3000000")`

#### Описание и логика работы
Приблизительная оценка пропускной способности, которую можно использовать при загрузке (в байтах в секунду).

Значение по умолчанию — `"3000000"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta drate "3000000" // вернуть/установить значение по умолчанию
```

---

### fraglog_public
`cvar fraglog_public(int, "1")`

#### Описание и логика работы
Включает поддержку запросов фраглогов без установления соединения.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta fraglog_public "1" // вернуть/установить значение по умолчанию
```

---

### gl_blacklist_generatemipmap
`cvar gl_blacklist_generatemipmap(int, "1")`

#### Описание и логика работы
Самостоятельное создание MIP-карт, вместо того, чтобы позволять это делать графическому драйверу.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta gl_blacklist_generatemipmap "1" // вернуть/установить значение по умолчанию
```

---

### host_speeds
`cvar host_speeds(int, "0")`

#### Описание и логика работы
Включает покадровый вывод времени работы основных стадий клиентского кадра в консоль. При ненулевом значении движок печатает суммарное время и отдельные столбцы idle, server, gfx и snd в микросекундах. Это диагностическая переменная для поиска узких мест производительности, а не игровая настройка.


#### Практическое использование
```cfg
seta host_speeds "0" // вернуть/установить значение по умолчанию
```

---

### net_enable_
`cvar net_enable_(int, "0")`

#### Описание и логика работы
Если этот параметр включен, принимайте запросы на соединение от механизма quake2-remaster на наших прослушиваемых udp-портах.  
Обратите внимание, что по умолчанию этот параметр отключен, поскольку он очень уязвим для атак приложений.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta net_enable_ "0" // вернуть/установить значение по умолчанию
```

---

### net_enable_dtls
`cvar net_enable_dtls(string, "")`

#### Описание и логика работы
Управляет поддержкой dtls на стороне сервера.

- **0:** dtls заблокирован, не объявлен.
- **1:** выбор на стороне клиента.
- **2:** используется там, где это возможно (рекомендуемая настройка).
- **3:** запретить клиенты, не использующие dtls (sv_port_tcp должен быть, например, tls://[::]:27500, чтобы также запретить незашифрованные TCP-соединения).

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta net_enable_dtls "" // вернуть/установить значение по умолчанию
```

---

### net_enable_qizmo
`cvar net_enable_qizmo(int, "1")`

#### Описание и логика работы
Включает поддержку на стороне сервера для «connect tcp://foo» или «connect tls://foo» (с net_enable_tls), а также tcp-соединений и совместимых qizmo.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta net_enable_qizmo "1" // вернуть/установить значение по умолчанию
```

---

### net_fakeloss
`cvar net_fakeloss(int, "0")`

#### Описание и логика работы
Имитирует потерю пакетов при приеме и отправке по шкале от 0 до 1.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta net_fakeloss "0" // вернуть/установить значение по умолчанию
```

---

### net_fakemtu
`cvar net_fakemtu(int, "0")`

#### Описание и логика работы
Уменьшает размер принимаемых пакетов.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta net_fakemtu "0" // вернуть/установить значение по умолчанию
```

---

### net_ice_debug
`cvar net_ice_debug(int, "0")`

#### Описание и логика работы
0: скрыть ненужные детали.

- **1:** показать новых кандидатов.
- **2:** показать каждый тест на подключение.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta net_ice_debug "0" // вернуть/установить значение по умолчанию
```

---

### net_ice_exchangeprivateips
`cvar net_ice_exchangeprivateips(int, "0")`

#### Описание и логика работы
Логическое значение. Если установлено значение 0, частные IP-адреса скрываются от ваших одноранговых узлов — будут доступны только адреса, определенные с другой стороны вашего маршрутизатора. Вам нужно установить значение 1 только в том случае, если mdns недоступен.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta net_ice_exchangeprivateips "0" // вернуть/установить значение по умолчанию
```

---

### net_ice_usewebrtc
`cvar net_ice_usewebrtc(string, "")`

#### Описание и логика работы
Используйте дополнительные накладные расходы webrtc, а не простой ICE. Это увеличивает размер пакетов и замедляет подключение, но совместимо с веб-портом.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta net_ice_usewebrtc "" // вернуть/установить значение по умолчанию
```

---

### net_upnpigp
`cvar net_upnpigp(int, "0")`

#### Описание и логика работы
Если установлено, позволяет использовать протокол upnp-igd для пробивания дыр в локальном блоке NAT.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta net_upnpigp "0" // вернуть/установить значение по умолчанию
```

---

### pausable
`cvar pausable(string, "")`

#### Описание и логика работы
Управляет тем, разрешена ли команда pause на сервере. Если строка пустая, движок автоматически разрешает паузу только вне deathmatch; если задано значение, берётся его целое 0/1. Спектаторы паузить игру не могут.


#### Практическое использование
```cfg
seta pausable "" // вернуть/установить значение по умолчанию
```

---

### qtv_password
`cvar qtv_password(string, "")`

#### Описание и логика работы
Переменная задаёт пароль для входящих QTV-подключений к MVD-стриму. Если строка пуста, либо соединение является reverse-подключением, авторизация не требуется; иначе код сравнивает либо открытый пароль, либо hash от `challenge + password` в зависимости от выбранного метода аутентификации.


#### Практическое использование
```cfg
seta qtv_password "" // вернуть/установить значение по умолчанию
```

---

### qtv_streamport
`cvar qtv_streamport(string, "")`

#### Описание и логика работы
Устаревший квар. Вместо этого используйте sv_port_tcp.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta qtv_streamport "" // вернуть/установить значение по умолчанию
```

---

### qtvcl_eztvextensions
`cvar qtvcl_eztvextensions(int, "1")`

#### Описание и логика работы
Разрешает отправку заголовка QTV_EZQUAKE_EXT при qtvplay-подключении к потоку, выбранному по SOURCE. При включении клиент дополнительно объявляет поддержку EZTV_DOWNLOAD, EZTV_SETINFO и EZTV_QTVUSERLIST и посылает USERINFO. Это нужно для расширенных возможностей EzQuake-совместимого QTV, таких как список пользователей и связанные сервисные команды.


#### Практическое использование
```cfg
seta qtvcl_eztvextensions "1" // вернуть/установить значение по умолчанию
```

---

### qtvcl_forceversion1
`cvar qtvcl_forceversion1(int, "0")`

#### Описание и логика работы
Принудительно переводит начальный запрос QTV на строку VERSION: 1.0 вместо VERSION: 1.1. Переменная нужна для совместимости со старыми QTV-серверами или прокси, которые не понимают более новую версию рукопожатия. На остальную сетевую логику клиента она не влияет.


#### Практическое использование
```cfg
seta qtvcl_forceversion1 "0" // вернуть/установить значение по умолчанию
```

---

### r_image_downloadsizelimit
`cvar r_image_downloadsizelimit(int, "131072")`

#### Описание и логика работы
Максимально допустимый размер файла изображений, загружаемых с URL-адреса в Интернете. 0 полностью отключает, а пустое значение не налагает ограничений.

Значение по умолчанию — `"131072"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta r_image_downloadsizelimit "131072" // вернуть/установить значение по умолчанию
```

---

### rate
`cvar rate(int, "30000")`

#### Описание и логика работы
Приблизительная оценка пропускной способности, которую можно использовать во время игры. Слишком высокое значение может привести к «раздуванию буфера».

Значение по умолчанию — `"30000"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta rate "30000" // вернуть/установить значение по умолчанию
```

---

### samelevel
`cvar samelevel(string, "")`

#### Описание и логика работы
Служебный cvar из server info, который общий C-код движка в этой ревизии сам не интерпретирует. Встроенные меню одиночной игры перед запуском новой карты явно очищают samelevel, чтобы не наследовать прежнее состояние; по историческому назначению переменная связана с повторным запуском текущего уровня и обычно читается игровой логикой мода.


#### Практическое использование
```cfg
seta samelevel "" // вернуть/установить значение по умолчанию
```

---

### sb_showfraglimit
`cvar sb_showfraglimit(int, "0")`

#### Описание и логика работы
Показывает в браузере серверов отдельную колонку fraglimit. Та же колонка становится кликабельным ключом сортировки; при выключении она не рисуется и не участвует в выборе столбца мышью.


#### Практическое использование
```cfg
seta sb_showfraglimit "0" // вернуть/установить значение по умолчанию
```

---

### sb_showtimelimit
`cvar sb_showtimelimit(int, "0")`

#### Описание и логика работы
Показывает в браузере серверов колонку timelimit. При включении значение timelimit выводится для каждой записи и может использоваться как поле сортировки по клику на заголовок.


#### Практическое использование
```cfg
seta sb_showtimelimit "0" // вернуть/установить значение по умолчанию
```

---

### skill
`cvar skill(string, "")`

#### Описание и логика работы
Задаёт уровень сложности, который движок учитывает при спавне сущностей на одиночных картах. Для Quake и Hexen II значения ниже 0.5 трактуются как easy, выше 1.5 — как hard, промежуточные — как medium; комментарий рядом с объявлением также указывает традиционный диапазон 0–3. Через меню это значение записывается перед стартом новой игры.


#### Практическое использование
```cfg
seta skill "" // вернуть/установить значение по умолчанию
```

---

### spawn
`cvar spawn(string, "")`

#### Описание и логика работы
Служебный cvar серверного управления, который движок почти не использует напрямую. По расположению рядом с skill и по отсутствию отдельной логики вокруг него он выступает как публикуемый параметр для игровой логики или стартовых скриптов, а не как обработчик клиентской команды spawn — сама команда реализована отдельно.


#### Практическое использование
```cfg
seta spawn "" // вернуть/установить значение по умолчанию
```

---

### sv_aim
`cvar sv_aim(int, "2")`

#### Описание и логика работы
Значение должно быть [cos](../37-quakec-builtins-reference/01-math-vector-builtins.md#cos)(угол), где угол — это наибольший допустимый угол, на который можно отклониться от направления, по которому должен был идти выстрел. Значения >1 таким образом отключают автоматическое прицеливание. Это можно переопределить с помощью setinfo [aim](../37-quakec-builtins-reference/03-entity-world-builtins.md#aim) 0.73.

Значение по умолчанию — `"2"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_aim "2" // вернуть/установить значение по умолчанию
```

---

### sv_autooffload
`cvar sv_autooffload(int, "0")`

#### Описание и логика работы
Автоматически запускать сервер в отдельном процессе, чтобы спорадические или постоянные замедления игрового кода не влияли на визуальную частоту кадров (аналогично команде mapcluster). Примечание. Разгруженные серверы имеют отдельные состояния cvar+command, что может усложнить использование.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_autooffload "0" // вернуть/установить значение по умолчанию
```

---

### sv_autosave
`cvar sv_autosave(int, "5")`

#### Описание и логика работы
Интервал автосохранений, в минутах. Установите значение 0, чтобы отключить автосохранение.

Значение по умолчанию — `"5"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_autosave "5" // вернуть/установить значение по умолчанию
```

---

### sv_chatfilter
`cvar sv_chatfilter(int, "0")`

#### Описание и логика работы
Включает фильтрацию символов '\r' и '\n' в обычном say и team say. При 1 сервер удаляет переводы строки из текста перед рассылкой, что мешает многострочному спаму и подделке системных сообщений. Для приватного tell аналогичная очистка выполняется и без этого cvar.


#### Практическое использование
```cfg
seta sv_chatfilter "0" // вернуть/установить значение по умолчанию
```

---

### sv_cheatpc
`cvar sv_cheatpc(int, "125")`

#### Описание и логика работы
Если клиент пытался потребовать больше, чем этот процент времени в течение любого периода мошенничества, клиент будет считаться обманщиком.

Значение по умолчанию — `"125"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_cheatpc "125" // вернуть/установить значение по умолчанию
```

---

### sv_cheats
`cvar sv_cheats(int, "0")`

#### Описание и логика работы
Главный серверный переключатель чит-режима, причём он помечен как CVAR_MAPLATCH и полноценно применяется после перезапуска/загрузки карты. При включении сервер выставляет *cheats=ON и разрешает команды/возможности, проверяемые через SV_MayCheat, например god, noclip и give; devmap/spdevmap также принудительно выставляют его в 1. При выключении одиночная игра всё равно сохраняет право на cheats через отдельную ветку SV_MayCheat, если на сервере только один клиентский слот.


#### Практическое использование
```cfg
seta sv_cheats "0" // вернуть/установить значение по умолчанию
```

---

### sv_cheatspeedchecktime
`cvar sv_cheatspeedchecktime(int, "30")`

#### Описание и логика работы
Интервал между каждой проверкой скорости.

Значение по умолчанию — `"30"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_cheatspeedchecktime "30" // вернуть/установить значение по умолчанию
```

---

### sv_cmdlikercon
`cvar sv_cmdlikercon(int, "0")`

#### Описание и логика работы
Разрешает специальный резервный путь для неизвестных пользовательских команд: если клиент авторизован через рейтинговую систему и код собран с SVRANKING, команда исполняется с уровнем доверия игрока почти как rcon/cmd. При 0 неизвестные команды после обычной обработки просто отклоняются. На сборках без SVRANKING практического эффекта не имеет.


#### Практическое использование
```cfg
seta sv_cmdlikercon "0" // вернуть/установить значение по умолчанию
```

---

### sv_compatiblehulls
`cvar sv_compatiblehulls(int, "1")`

#### Описание и логика работы
Определяет выбор collision hull для трассировки движения на сервере. При ненулевом значении движок принудительно использует hull 0, что даёт совместимое со старым Quake или Half-Life поведение; при 0 подбирается наиболее подходящий hull по размерам объекта. Влияет именно на серверную физику и столкновения.


#### Практическое использование
```cfg
seta sv_compatiblehulls "1" // вернуть/установить значение по умолчанию
```

---

### sv_csqc_progname
`cvar sv_csqc_progname(string, "csprogs.dat")`

#### Описание и логика работы
Задаёт имя файла CSQC-программы, которую сервер пытается загрузить и анонсировать клиентам через *csprogs, *csprogssize и *csprogsname. Если файл существует, его checksum и размер попадают в server info, а клиентам при prespawn может быть предложена загрузка этого же файла. Пустое значение фактически отключает такую публикацию.


#### Практическое использование
```cfg
seta sv_csqc_progname "csprogs.dat" // вернуть/установить значение по умолчанию
```

---

### sv_csqcdebug
`cvar sv_csqcdebug(int, "0")`

#### Описание и логика работы
Введите информацию о размере пакета для данных, направляемых в csqc.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_csqcdebug "0" // вернуть/установить значение по умолчанию
```

---

### sv_demoAutoCompress
`cvar sv_demoAutoCompress(string, "")`

#### Описание и логика работы
Указывает, следует ли сжимать демо-версии во время их записи.

- **0=** без сжатия.
- **1=** сжатие gzip.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_demoAutoCompress "" // вернуть/установить значение по умолчанию
```

---

### sv_demoAutoPrefix
`cvar sv_demoAutoPrefix(string, "auto_")`

#### Описание и логика работы
Префикс автоматически записываемых демок сервера. Он используется только в `SV_MVD_AutoRecord()`, где имя файла составляется как `sv_demoDir/sv_demoAutoPrefix + svs.name + timestamp` перед добавлением расширения `.mvd` или `.mvd.gz`.


#### Практическое использование
```cfg
seta sv_demoAutoPrefix "auto_" // вернуть/установить значение по умолчанию
```

---

### sv_demoAutoRecord
`cvar sv_demoAutoRecord(int, "0")`

#### Описание и логика работы
Если установлено, автоматически записывать демо-версии.  
-1: запись только при подключении клиента.  
1+: запись, когда на сервере находится определенное количество активных игроков (или при подключении к удаленному серверу).

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_demoAutoRecord "0" // вернуть/установить значение по умолчанию
```

---

### sv_demoCacheSize
`cvar sv_demoCacheSize(string, "0x80000")`

#### Описание и логика работы
Переменная задаёт размер буфера для кэшируемой или потоковой записи MVD в память. При инициализации назначения значение меньше `0x8000` принудительно повышается до этого минимума, после чего по нему выделяются `cache` и, при threaded-записи, `altcache`.


#### Практическое использование
```cfg
seta sv_demoCacheSize "0x80000" // вернуть/установить значение по умолчанию
```

---

### sv_demoDir
`cvar sv_demoDir(string, "demos")`

#### Описание и логика работы
Переменная задаёт основной каталог для записи, перечисления, удаления и выдачи серверных демок. Callback `SV_DemoDir_Callback()` запрещает пустые, абсолютные и UNC-пути, а также любые вхождения `..` кроме единственного ведущего перехода на уровень выше; при нарушении значение откатывается к engine value.


#### Практическое использование
```cfg
seta sv_demoDir "demos" // вернуть/установить значение по умолчанию
```

---

### sv_demoDirAlt
`cvar sv_demoDirAlt(string, "")`

#### Описание и логика работы
Предоставляет резервное имя каталога для загрузки демо-версии, если sv_demoDir не содержит запрошенную демо-версию.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_demoDirAlt "" // вернуть/установить значение по умолчанию
```

---

### sv_demoExtraNames
`cvar sv_demoExtraNames(string, "")`

#### Описание и логика работы
Переменная влияет на автогенерацию имён демок в командном teamplay-режиме. Если её числовое значение больше нуля, код добавляет в имя не только названия команд, но и теги с именами игроков по командам; иначе используется более короткая схема `team1_vs_team2_map`.


#### Практическое использование
```cfg
seta sv_demoExtraNames "" // вернуть/установить значение по умолчанию
```

---

### sv_demoPings
`cvar sv_demoPings(int, "10")`

#### Описание и логика работы
Интервал между обновлениями пинга в mvds

Значение по умолчанию — `"10"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_demoPings "10" // вернуть/установить значение по умолчанию
```

---

### sv_demoPrefix
`cvar sv_demoPrefix(string, "")`

#### Описание и логика работы
Переменная добавляет общий префикс к базовому имени демки перед расширением. Она применяется как при ручной записи через `record`, так и при автогенерации имён в `SV_MVD_Record_f()`, после очистки имени функцией `SV_CleanName()`.


#### Практическое использование
```cfg
seta sv_demoPrefix "" // вернуть/установить значение по умолчанию
```

---

### sv_demoSuffix
`cvar sv_demoSuffix(string, "")`

#### Описание и логика работы
Переменная добавляет общий суффикс к базовому имени демки перед расширением файла. Код учитывает длину суффикса при ограничении максимальной длины имени, чтобы затем безопасно дописать `.mvd` или `.mvd.gz`.


#### Практическое использование
```cfg
seta sv_demoSuffix "" // вернуть/установить значение по умолчанию
```

---

### sv_demotxt
`cvar sv_demotxt(int, "1")`

#### Описание и логика работы
Переменная управляет созданием сопутствующего текстового файла рядом с записываемой демкой. При ненулевом значении файл создаётся, причём режим `2` оставляет его пустым для последующего заполнения модом через [`sv_demoinfoadd`](../44-cli-commands-reference/04-server-multiplayer-commands.md#sv_demoinfoadd), а остальные ненулевые значения сразу записывают дату, карту, режимы игры и составы команд; при 0 существующий `.txt` удаляется.


#### Практическое использование
```cfg
seta sv_demotxt "1" // вернуть/установить значение по умолчанию
```

---

### sv_dlURL
`cvar sv_dlURL(string, "")`

#### Описание и логика работы
Предоставляет клиентам внешний URL-адрес, по которому они могут получать pk3s/пакеты с внешнего http-сервера вместо необходимости загрузки через udp.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_dlURL "" // вернуть/установить значение по умолчанию
```

---

### sv_floodprotect
`cvar sv_floodprotect(int, "1")`

#### Описание и логика работы
Главный переключатель серверной защиты от флуда для чата и, при отдельной настройке, suicide. При 0 проверки полностью отключаются; при 1 учитываются sv_floodprotect_messages, sv_floodprotect_interval и sv_floodprotect_silencetime. Если лимит превышен, игрок временно теряет право говорить.


#### Практическое использование
```cfg
seta sv_floodprotect "1" // вернуть/установить значение по умолчанию
```

---

### sv_floodprotect_interval
`cvar sv_floodprotect_interval(int, "4")`

#### Описание и логика работы
Задаёт длину окна в секундах, внутри которого оценивается частота сообщений. Счётчик защиты от флуда убывает линейно по мере ожидания и сравнивается с лимитом sv_floodprotect_messages, так что большее значение делает антиспам строже на длинной дистанции. Нулевое или отрицательное значение фактически отключает механизм вместе с лимитом сообщений.


#### Практическое использование
```cfg
seta sv_floodprotect_interval "4" // вернуть/установить значение по умолчанию
```

---

### sv_floodprotect_messages
`cvar sv_floodprotect_messages(int, "4")`

#### Описание и логика работы
Определяет, сколько сообщений можно накопить в пределах окна sv_floodprotect_interval до срабатывания блокировки. Когда внутренний счётчик превышает это значение, игрок переводится в режим молчания на sv_floodprotect_silencetime секунд. Нулевое или отрицательное значение отключает проверку.


#### Практическое использование
```cfg
seta sv_floodprotect_messages "4" // вернуть/установить значение по умолчанию
```

---

### sv_floodprotect_sendmessage
`cvar sv_floodprotect_sendmessage(string, "")`

#### Описание и логика работы
Текст, который сервер отправляет игроку в момент срабатывания защиты от флуда. Сообщение выводится в чат как "FloodProt: ..." только если строка не пуста. Позволяет заменить немой временный мьют на понятное объяснение причины блокировки.


#### Практическое использование
```cfg
seta sv_floodprotect_sendmessage "" // вернуть/установить значение по умолчанию
```

---

### sv_floodprotect_silencetime
`cvar sv_floodprotect_silencetime(int, "10")`

#### Описание и логика работы
Длительность наказания в секундах после превышения лимита защиты от флуда. Именно на это время сервер выставляет client->lockedtill и блокирует новые сообщения, а при включённом sv_floodprotect_suicide — ещё и команду suicide. Чем больше значение, тем дольше игрок остаётся без права говорить.


#### Практическое использование
```cfg
seta sv_floodprotect_silencetime "10" // вернуть/установить значение по умолчанию
```

---

### sv_floodprotect_suicide
`cvar sv_floodprotect_suicide(int, "1")`

#### Описание и логика работы
Подключает команду suicide к тому же счётчику защиты от флуда, что и чат. При 1 частые suicide-команды тоже могут быть временно запрещены, и каждая успешная попытка увеличивает flood-счётчик; при 0 защита применяется только к общению. Полезно против bind-ов, которые спамят suicide как сетевую или логическую нагрузку.


#### Практическое использование
```cfg
seta sv_floodprotect_suicide "1" // вернуть/установить значение по умолчанию
```

---

### sv_ftp
`cvar sv_ftp(int, "0")`

#### Описание и логика работы
Переменная включает встроенный FTP-сервер движка. В `IWebRun()` её значение преобразуется в булево и передаётся в `FTP_ServerRun()`, а при неудачном старте код печатает сообщение об ошибке и сам сбрасывает cvar обратно в 0.


#### Практическое использование
```cfg
seta sv_ftp "0" // вернуть/установить значение по умолчанию
```

---

### sv_ftp_port
`cvar sv_ftp_port(int, "21")`

#### Описание и логика работы
Переменная задаёт порт прослушивания встроенного FTP-сервера и передаётся вторым аргументом в `FTP_ServerRun()`. Она влияет только на управляющее соединение; отдельный диапазон для active FTP задаётся другим cvar `sv_ftp_port_range`.


#### Практическое использование
```cfg
seta sv_ftp_port "21" // вернуть/установить значение по умолчанию
```

---

### sv_ftp_port_range
`cvar sv_ftp_port_range(int, "0")`

#### Описание и логика работы
Указывает диапазон портов сервера для создания сокетов прослушивания для «активных» ftp-соединений, чтобы обойти проблемы NAT/брандмауэра.  
Большинство FTP-клиентов должны использовать пассивные соединения, но все же есть некоторые препятствия, такие как Windows.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_ftp_port_range "0" // вернуть/установить значение по умолчанию
```

---

### sv_fulllevel
`cvar sv_fulllevel(int, "51")`

#### Описание и логика работы
Учетные записи пользователей с уровнем доступа выше этого могут писать куда угодно, включая игровой каталог. Обратите внимание, что устанавливать этот низкий уровень невероятно рискованно. Пустое значение будет пониматься как никогда не дающее это разрешение.

Значение по умолчанию — `"51"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_fulllevel "51" // вернуть/установить значение по умолчанию
```

---

### sv_fullredirect
`cvar sv_fullredirect(string, "")`

#### Описание и логика работы
Это ip:порт для перенаправления игроков, когда сервер заполнен.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_fullredirect "" // вернуть/установить значение по умолчанию
```

---

### sv_gameplayfix_honest_tracelines
`cvar sv_gameplayfix_honest_tracelines(int, "1")`

#### Описание и логика работы
Исправляет старое поведение [traceline](../37-quakec-builtins-reference/03-entity-world-builtins.md#traceline)/[tracebox](../37-quakec-builtins-reference/03-entity-world-builtins.md#tracebox), когда трасса, начавшаяся внутри solid, могла вернуть trace_fraction = 1 и выглядеть как полностью свободная. При 1 сохраняется реальный результат столкновения; при 0 эмулируется старое совместимое поведение. Влияет на глобальные результаты trace, которые серверный QC получает после вызовов traceline и tracebox.


#### Практическое использование
```cfg
seta sv_gameplayfix_honest_tracelines "1" // вернуть/установить значение по умолчанию
```

---

### sv_gameplayfix_radialmaxvelocity
`cvar sv_gameplayfix_radialmaxvelocity(int, "0")`

#### Описание и логика работы
Применяет максимальную скорость радиально, а не аксиально.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_gameplayfix_radialmaxvelocity "0" // вернуть/установить значение по умолчанию
```

---

### sv_gameplayfix_setmodelrealbox
`cvar sv_gameplayfix_setmodelrealbox(int, "1")`

#### Описание и логика работы
Vanilla [setmodel](../37-quakec-builtins-reference/03-entity-world-builtins.md#setmodel) установит для объекта жестко запрограммированный размер для моделей, отличных от BSP. Вместо этого эта переменная всегда будет использовать реальный размер модели, но потребует, чтобы сервер действительно загружал модель.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_gameplayfix_setmodelrealbox "1" // вернуть/установить значение по умолчанию
```

---

### sv_gameplayfix_setmodelsize_qw
`cvar sv_gameplayfix_setmodelsize_qw(int, "0")`

#### Описание и логика работы
Встроенная функция setmodel также будет действовать как размер набора для модов QuakeWorld.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_gameplayfix_setmodelsize_qw "0" // вернуть/установить значение по умолчанию
```

---

### sv_gameplayfix_spawnbeforethinks
`cvar sv_gameplayfix_spawnbeforethinks(int, "0")`

#### Описание и логика работы
Исправлена ​​проблема, из-за которой игрок думал, что (включая Pre+Post) можно вызвать перед [PutClientInServer](../37-quakec-builtins-reference/00-entry-points.md#putclientinserver). К сожалению, по крайней мере один мод зависит от того, чтобы PreThink был вызван первым, чтобы правильно определить позиции появления.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_gameplayfix_spawnbeforethinks "0" // вернуть/установить значение по умолчанию
```

---

### sv_gameplayfix_stepdown
`cvar sv_gameplayfix_stepdown(int, "0")`

#### Описание и логика работы
Попытайтесь спускаться по ступенькам, а не только подниматься по ним. Влияет на непрогнозируемый movetype_walk.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_gameplayfix_stepdown "0" // вернуть/установить значение по умолчанию
```

---

### sv_gamespeed
`cvar sv_gamespeed(int, "1")`

#### Описание и логика работы
Масштабирует ход серверного времени и тем самым замедляет или ускоряет весь игровой симулятор. Значение 1 означает нормальную скорость, любые значения ниже 0.1 или ровно 1 принудительно нормализуются обратно к 1; при изменении cvar корректируется *gamespeed и базовое starttime сервера. Текущее значение также отправляется клиентам как STAT_MOVEVARS_TIMESCALE.


#### Практическое использование
```cfg
seta sv_gamespeed "1" // вернуть/установить значение по умолчанию
```

---

### sv_getrealip
`cvar sv_getrealip(int, "0")`

#### Описание и логика работы
Попытайтесь получить более надежный IP-адрес для клиентов, а не только их прокси.

- **0:** Не пытайтесь.
- **1:** Ненадежные проверки.
- **2:** Подтвердите, если возможно.
- **3:** Обязательная проверка.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_getrealip "0" // вернуть/установить значение по умолчанию
```

---

### sv_hideinactivegames
`cvar sv_hideinactivegames(int, "1")`

#### Описание и логика работы
Не показывать известные игры, у которых на данный момент нет серверов, в html-списках.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_hideinactivegames "1" // вернуть/установить значение по умолчанию
```

---

### sv_highchars
`cvar sv_highchars(int, "1")`

#### Описание и логика работы
Разрешает ли сервер принимать в именах символы из второго набора шрифта. При 0 такие символы отбрасываются на этапе очистки строки вместе с другими скрытыми или пиктографическими кодами; при 1 остаются разрешены. Практически это фильтр допустимых никнеймов.


#### Практическое использование
```cfg
seta sv_highchars "1" // вернуть/установить значение по умолчанию
```

---

### sv_http
`cvar sv_http(int, "0")`

#### Описание и логика работы
Переменная включает встроенный HTTP-сервер движка. В `IWebRun()` её булево значение передаётся в `HTTP_ServerPoll()`, а при провале инициализации движок уведомляет об этом и автоматически сбрасывает cvar в 0.


#### Практическое использование
```cfg
seta sv_http "0" // вернуть/установить значение по умолчанию
```

---

### sv_http_port
`cvar sv_http_port(int, "80")`

#### Описание и логика работы
Переменная задаёт TCP-порт встроенного HTTP-сервера. Значение напрямую передаётся в `HTTP_ServerPoll()` и тем самым определяет, на каком порту движок пытается открыть web-интерфейс.


#### Практическое использование
```cfg
seta sv_http_port "80" // вернуть/установить значение по умолчанию
```

---

### sv_listen_q3
`cvar sv_listen_q3(int, "0")`

#### Описание и логика работы
Включает обработку Quake 3-совместимых подключений и пакетов QW-over-Q3. При 0 сервер отклоняет q3 connect и не отвечает соответствующей ветке challenge или packet handling; при 1 такие клиенты допускаются и обслуживаются обработчиком q3->sv. Относится к сетевой совместимости сервера, а не к игровым правилам.


#### Практическое использование
```cfg
seta sv_listen_q3 "0" // вернуть/установить значение по умолчанию
```

---

### sv_listen_qw
`cvar sv_listen_qw(int, "1")`

#### Описание и логика работы
Указывает, разрешено ли подключение обычным клиентам.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_listen_qw "1" // вернуть/установить значение по умолчанию
```

---

### sv_loadentfiles
`cvar sv_loadentfiles(int, "1")`

#### Описание и логика работы
Разрешает подмену встроенного entity lump карты внешним файлом. При ненулевом значении движок сначала пытается загрузить .ent, а затем для совместимости .edo; если внешний файл не найден, используются сущности из самой карты. Переменная влияет именно на чтение внешних описаний сущностей, поэтому позволяет менять спавны, триггеры и прочие entity без пересборки BSP.


#### Практическое использование
```cfg
seta sv_loadentfiles "1" // вернуть/установить значение по умолчанию
```

---

### sv_mapcheck
`cvar sv_mapcheck(int, "1")`

#### Описание и логика работы
Проверяет, совпадает ли контрольная сумма карты у клиента с серверной worldmodel во время этапа prespawn. При включении и обнаружении несовпадения сервер выводит причину, отдаёт kickfile и разрывает соединение; для некоторых NetQuake-подобных протоколов принудительная проверка невозможна, и тогда остаётся лишь предупреждение в консоль. Используется как защита от неверных или подменённых BSP.


#### Практическое использование
```cfg
seta sv_mapcheck "1" // вернуть/установить значение по умолчанию
```

---

### sv_master
`cvar sv_master(int, "0")`

#### Описание и логика работы
Переводит процесс в режим встроенного master server, если сборка собрана с поддержкой SV_MASTER. При ненулевом значении каждый серверный кадр вызывает SVM_Think(), который обслуживает heartbeats, списки игр и регистрацию серверов разных протоколов. Обычному игровому серверу эта переменная не нужна.


#### Практическое использование
```cfg
seta sv_master "0" // вернуть/установить значение по умолчанию
```

---

### sv_masterport
`cvar sv_masterport(string, " ")`

#### Описание и логика работы
Задаёт UDP-порт или список UDP-портов, которые master-сервер открывает для приёма heartbeat/serverinfo-пакетов и запросов клиентов. При изменении значения вызывается callback, который заново добавляет указанные адреса в сетевую коллекцию сокетов. Практически это позволяет перенести master-службу на другие UDP-порты или слушать несколько портов сразу.


#### Практическое использование
```cfg
seta sv_masterport " " // вернуть/установить значение по умолчанию
```

---

### sv_masterport_tcp
`cvar sv_masterport_tcp(string, "")`

#### Описание и логика работы
Задаёт TCP-порт или список TCP-портов, которые master-сервер открывает для stream-подключений. Значение применяется через отдельный callback и добавляется в ту же коллекцию слушающих сокетов, но уже с типом NP_STREAM. Нужен, если HTTP/ICEBroker-составляющая master-службы должна слушать нестандартный TCP-порт.


#### Практическое использование
```cfg
seta sv_masterport_tcp "" // вернуть/установить значение по умолчанию
```

---

### sv_maxaim
`cvar sv_maxaim(int, "22")`

#### Описание и логика работы
Максимально допустимый угол автоприцеливания.

Значение по умолчанию — `"22"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_maxaim "22" // вернуть/установить значение по умолчанию
```

---

### sv_nopvs
`cvar sv_nopvs(int, "0")`

#### Описание и логика работы
Установите значение 1, чтобы игнорировать pvs на сервере. Это может сделать взлом стены более опасным, поэтому его следует использовать только для отладки.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_nopvs "0" // вернуть/установить значение по умолчанию
```

---

### sv_phs
`cvar sv_phs(int, "1")`

#### Описание и логика работы
Если 1, не используйте физ. Обычно вместо этого лучше использовать sv_calcphs и оставлять значение 1.

Значение по умолчанию — `"1"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_phs "1" // вернуть/установить значение по умолчанию
```

---

### sv_ping_ignorepl
`cvar sv_ping_ignorepl(int, "0")`

#### Описание и логика работы
Если установлено значение 1, время пинга, сообщаемое для игроков, будет игнорировать влияние потери пакетов на время пинга. 0 немного честнее, но менее полезен для диагностики соединения.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_ping_ignorepl "0" // вернуть/установить значение по умолчанию
```

---

### sv_playermodelchecks
`cvar sv_playermodelchecks(int, "0")`

#### Описание и логика работы
Сверяет userinfo-поля pmodel и emodel с контрольными суммами серверных моделей player и eyes. При несовпадении сервер только рассылает предупреждение, но не кикает клиента. Это диагностическая проверка на подменённые модели игрока, а не жёсткий принудительный контроль.


#### Практическое использование
```cfg
seta sv_playermodelchecks "0" // вернуть/установить значение по умолчанию
```

---

### sv_port
`cvar sv_port(string, "Port number to list on for inbound udp-based connections (including dtls variants). Can be a list for multiple ports. If ips are included then binds to that specific interface. Whether any specific protocol is accepted depends upon other settings.")`

#### Описание и логика работы
Задаёт UDP-порт или список портов, на которых сервер слушает входящие IPv4-подключения. Строка передаётся напрямую в сетевую коллекцию, поэтому можно указывать не только номер порта, но и адреса с портами для привязки к конкретным интерфейсам. Изменение переменной приводит к переоткрытию соответствующего слушающего сокета при следующем применении callback'а или инициализации сервера.


#### Практическое использование
```cfg
seta sv_port "Port number to list on for inbound udp-based connections (including dtls variants). Can be a list for multiple ports. If ips are included then binds to that specific interface. Whether any specific protocol is accepted depends upon other settings." // вернуть/установить значение по умолчанию
```

---

### sv_port_ipv6
`cvar sv_port_ipv6(string, "")`

#### Описание и логика работы
Порт, который будет использоваться для входящих соединений ipv6 udp. Из-за гибридных сокетов это может не понадобиться. Если хотите, вы можете указать адрес ipv4:порт для второго порта ipv4.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_port_ipv6 "" // вернуть/установить значение по умолчанию
```

---

### sv_port_ipx
`cvar sv_port_ipx(string, "")`

#### Описание и логика работы
Если вам нужен этот параметр, значит, вы делаете что-то странное/древнее.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_port_ipx "" // вернуть/установить значение по умолчанию
```

---

### sv_port_natpmp
`cvar sv_port_natpmp(string, "If set (typically to 5351), automatically configures your router's port forwarding. You can instead specify the full ip address of your router (192.168.1.1:5351 for example). Your router must have NAT-PMP supported and enabled.")`

#### Описание и логика работы
Включает попытку автоматической настройки проброса порта через NAT-PMP. При непустом значении движок добавляет специальную цель вида natpmp://... в сетевую коллекцию; это позволяет попросить маршрутизатор открыть внешний доступ к серверу. Эффект зависит от поддержки NAT-PMP роутером, а в dedicated-конфигурации значение по умолчанию оставлено пустым, чтобы не менять сеть без явного запроса.


#### Практическое использование
```cfg
seta sv_port_natpmp "If set (typically to 5351), automatically configures your router's port forwarding. You can instead specify the full ip address of your router (192.168.1.1:5351 for example). Your router must have NAT-PMP supported and enabled." // вернуть/установить значение по умолчанию
```

---

### sv_port_rtc
`cvar sv_port_rtc(string, "/")`

#### Описание и логика работы
Здесь указывается URL-адрес брокера, с которого можно получать клиентов. Если имя хоста опущено, оно будет взято из манифеста. Если этот параметр опущен, служба брокера рандомизирует часть ресурса, поэтому обязательно сообщите своим друзьям путь, указанный, например, в статусе, а не только в этом значении квара. Или просто установите для него значение «rtc:///example» и сообщите клиентам подключаться к тому же значению sservevalue.

Значение по умолчанию — `"/"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_port_rtc "/" // вернуть/установить значение по умолчанию
```

---

### sv_port_tcp
`cvar sv_port_tcp(string, "")`

#### Описание и логика работы
Номер порта для входящих TCP-соединений (включая варианты tls). Может быть списком нескольких портов. Если ips включен, то привязывается к этому конкретному интерфейсу. Принятие какого-либо конкретного протокола зависит от других настроек.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_port_tcp "" // вернуть/установить значение по умолчанию
```

---

### sv_port_tcp6
`cvar sv_port_tcp6(string, "")`

#### Описание и логика работы
Эквивалент sv_port_tcp, но предполагается, что номера портов — только ipv6. Актуально только в том случае, если не используются гибридные сокеты.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_port_tcp6 "" // вернуть/установить значение по умолчанию
```

---

### sv_port_unix
`cvar sv_port_unix(string, "@qsock.fte")`

#### Описание и логика работы
Задаёт адрес Unix-domain datagram socket для локальной связи: на Linux по умолчанию используется абстрактный адрес @qsock.fte, на других Unix-платформах — файловый путь вроде /tmp/qsock.fte. Однако в текущем коде регистрация и принудительный callback для этой cvar закомментированы, поэтому настройка фактически не вводится в обычный набор networking cvar-ов и обычно не активна без правок кода. Это скорее заготовка под локальный IPC, чем активно используемый сетевой порт.


#### Практическое использование
```cfg
seta sv_port_unix "@qsock.fte" // вернуть/установить значение по умолчанию
```

---

### sv_progs
`cvar sv_progs(string, "")`

#### Описание и логика работы
Указывает имя файла игрового кода, который будет использоваться на стороне сервера. Пусто означает автоопределение — обычно загружаются либо программы, либо qwprogs в зависимости от приоритета игрового каталога, а затем на основе настроек Deathmatch.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_progs "" // вернуть/установить значение по умолчанию
```

---

### sv_protocol_nq
`cvar sv_protocol_nq(string, "")`

#### Описание и логика работы
Указывает протокол по умолчанию, который будет использоваться для новых клиентов NQ. Это актуально только для клиентов, которые не сообщают о поддерживаемых протоколах. Поддерживаемые значения:  
0 = автоопределение  
15 = vanilla  
666 = fitzquake  
999 = протокол rmq  
Cvar sv_bigcoords вызывает обновление по мере необходимости.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_protocol_nq "" // вернуть/установить значение по умолчанию
```

---

### sv_pupglow
`cvar sv_pupglow(string, "")`

#### Описание и логика работы
Инструктирует клиентов включить импульсное включение питания в стиле hexen2.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_pupglow "" // вернуть/установить значение по умолчанию
```

---

### sv_pure
`cvar sv_pure(string, "")`

#### Описание и логика работы
Самая злая переменная в мире, многие клиенты игнорируют ее.  
0=стандартные правила Quake.  
1=клиенты должны отдавать предпочтение файлам в пакетах, присутствующих на сервере.  
2=клиенты должны использовать *только* файлы в пакетах, присутствующих на сервере.  
Из-за различий в Quake 1.01/1.06 настройка 2 работает только при общем количестве преобразований.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_pure "" // вернуть/установить значение по умолчанию
```

---

### sv_readlevel
`cvar sv_readlevel(int, "0")`

#### Описание и логика работы
Переменная задаёт минимальный `trustlevel`, необходимый для read-only доступа к встроенным HTTP или FTP сервисам. При значении 0 код отдельно разрешает анонимное чтение для пустого имени пользователя или `anonymous`, а при больших значениях доступ получают только игроки, чей `Rank_GetPlayerInfo()` возвращает достаточный уровень доверия.


#### Практическое использование
```cfg
seta sv_readlevel "0" // вернуть/установить значение по умолчанию
```

---

### sv_realip_kick
`cvar sv_realip_kick(int, "0")`

#### Описание и логика работы
Кикает клиентов, если их реалип не может быть проверен на уровне, указанном в sv_getrealip.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_realip_kick "0" // вернуть/установить значение по умолчанию
```

---

### sv_realip_timeout
`cvar sv_realip_timeout(int, "10")`

#### Описание и логика работы
Лимит времени в секундах на подтверждение real IP клиента через механизм sv_getrealip. Если за это время проверка не завершается, либо протокол или тип адреса не поддерживается, сервер прекращает ожидание, сообщает об этом клиенту и может кикнуть его в зависимости от sv_realip_kick. Полезно для серверов за прокси или NAT, где важно знать настоящий адрес.


#### Практическое использование
```cfg
seta sv_realip_timeout "10" // вернуть/установить значение по умолчанию
```

---

### sv_realiphostname_ipv4
`cvar sv_realiphostname_ipv4(string, "")`

#### Описание и логика работы
Это общедоступный IP-адрес:порт сервера. Это необходимо для работы RealIP, когда автоматически определенный/локальный IP-адрес не маршрутизируется глобально.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_realiphostname_ipv4 "" // вернуть/установить значение по умолчанию
```

---

### sv_realiphostname_ipv6
`cvar sv_realiphostname_ipv6(string, "")`

#### Описание и логика работы
Это общедоступный IP-адрес:порт сервера. Это необходимо для работы RealIP, когда автоматически определенный/локальный IP-адрес не маршрутизируется глобально.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_realiphostname_ipv6 "" // вернуть/установить значение по умолчанию
```

---

### sv_resetparms
`cvar sv_resetparms(int, "0")`

#### Описание и логика работы
Управляет тем, сбрасывать ли spawn parms у уже известных серверу игроков. При включении сервер при повторном входе берёт свежие значения через SV_GetNewSpawnParms(); при 0 старается восстановить сохранённые ranking или spawn-параметры и обнуляет только неподдерживаемую часть. Нюанс проявляется на серверах, где состояние игрока переживает реконнект.


#### Практическое использование
```cfg
seta sv_resetparms "0" // вернуть/установить значение по умолчанию
```

---

### sv_savefmt
`cvar sv_savefmt(string, "")`

#### Описание и логика работы
Указывает формат сохраненной игры.

- **0=** устаревший.
- **1=** fte
- **2=** двоичный.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_savefmt "" // вернуть/установить значение по умолчанию
```

---

### sv_showconnectionlessmessages
`cvar sv_showconnectionlessmessages(int, "0")`

#### Описание и логика работы
Отобразите строку, описывающую каждое сообщение без установления соединения, поступающее на сервер. В первую очередь это функция отладки, но она также потенциально полезна для администраторов.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_showconnectionlessmessages "0" // вернуть/установить значение по умолчанию
```

---

### sv_showpredloss
`cvar sv_showpredloss(int, "0")`

#### Описание и логика работы
Печатайте сообщения всякий раз, когда входные кадры игнорируются или форсируются на стороне сервера, чтобы предотвратить использование чит-кодов или читов при наведении курсора. Любые такие отпечатки будут сопровождаться промахами в прогнозировании в указанном клиенте.

Значение по умолчанию — `"0"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_showpredloss "0" // вернуть/установить значение по умолчанию
```

---

### sv_sortlist
`cvar sv_sortlist(int, "3")`

#### Описание и логика работы
Управляет сортировкой вывода http:

- **0:** не беспокойтесь
- **1:** клиенты, затем адрес
- **2:** имя хоста, затем адрес
- **3:** клиенты, затем имя хоста, затем адрес
- **4:** просто адрес

Значение по умолчанию — `"3"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_sortlist "3" // вернуть/установить значение по умолчанию
```

---

### sv_specprint
`cvar sv_specprint(int, "3")`

#### Описание и логика работы
Битовое поле, которое контролирует, какие события игрока видят зрители при отслеживании этого игрока.  
&1: зрители будут видеть центральные отпечатки.  
&2: зрители будут видеть спринты (сообщения о пикапе и т. д.).  
&4: зрители будут получать консольные команды, это потенциально рискованно.  
Отдельные зрители могут использовать 'setinfo sp foo', чтобы ограничить этот параметр.

Значение по умолчанию — `"3"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_specprint "3" // вернуть/установить значение по умолчанию
```

---

### sv_spectalk
`cvar sv_spectalk(int, "1")`

#### Описание и логика работы
Определяет, могут ли наблюдатели общаться с обычными игроками в общих каналах. При 0 [spectator](07-system-misc-cvars.md#spectator) text и voice ограничиваются кругом наблюдателей; при 1 их обычные сообщения и voice могут идти всем, хотя team-общение по-прежнему остаётся раздельным. Влияет и на то, как такие сообщения попадают в MVD и маршрутизацию voice.


#### Практическое использование
```cfg
seta sv_spectalk "1" // вернуть/установить значение по умолчанию
```

---

### sv_sql_defaultdb
`cvar sv_sql_defaultdb(string, "")`

#### Описание и логика работы
Переменная служит значением по умолчанию для четвёртого параметра подключения в QC builtin [`sqlconnect`](../37-quakec-builtins-reference/06-files-database-builtins.md#sqlconnect), если мод его не передал. Для MySQL эта строка уходит как имя базы данных, а для SQLite при выборе драйвера она преобразуется в путь вида `sqlite/<name>.db`, кроме специального случая `:memory:`.


#### Практическое использование
```cfg
seta sv_sql_defaultdb "" // вернуть/установить значение по умолчанию
```

---

### sv_sql_driver
`cvar sv_sql_driver(string, "")`

#### Описание и логика работы
Переменная задаёт драйвер SQL по умолчанию для `sqlconnect`, когда мод не указал его явно. `SQL_NewServer()` понимает строки `mysql` и `sqlite`, а при пустом выборе автоматически предпочитает доступный SQLite и только затем MySQL.


#### Практическое использование
```cfg
seta sv_sql_driver "" // вернуть/установить значение по умолчанию
```

---

### sv_sql_host
`cvar sv_sql_host(string, "127.0.0.1")`

#### Описание и логика работы
Переменная служит значением по умолчанию для первого параметра подключения в `sqlconnect`. В ветке MySQL эта строка передаётся как host и может содержать суффикс `:port`, который `sql_serverworker()` временно отделяет и конвертирует в числовой порт; для SQLite данный параметр роли не играет.


#### Практическое использование
```cfg
seta sv_sql_host "127.0.0.1" // вернуть/установить значение по умолчанию
```

---

### sv_sql_password
`cvar sv_sql_password(string, "")`

#### Описание и логика работы
Переменная служит значением по умолчанию для третьего параметра подключения в QC builtin `sqlconnect`, если мод не передал пароль явно. Дальнейшее использование зависит от выбранного драйвера: для MySQL строка передаётся в `mysql_real_connect()`, а для SQLite фактически игнорируется.


#### Практическое использование
```cfg
seta sv_sql_password "" // вернуть/установить значение по умолчанию
```

---

### sv_sql_username
`cvar sv_sql_username(string, "")`

#### Описание и логика работы
Переменная служит значением по умолчанию для второго параметра подключения в QC builtin `sqlconnect`. В коде `PF_sqlconnect()` она подставляется только при пустом аргументе со стороны QC, после чего для MySQL уходит в `mysql_real_connect()`, а для SQLite остаётся неиспользуемой формальностью интерфейса.


#### Практическое использование
```cfg
seta sv_sql_username "" // вернуть/установить значение по умолчанию
```

---

### sv_userinfo_bytelimit
`cvar sv_userinfo_bytelimit(int, "8192")`

#### Описание и логика работы
Это максимальное количество байтов, которое может храниться в информации пользователя каждого пользователя. Обратите внимание, что это также включает в себя имена ключей.

Значение по умолчанию — `"8192"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_userinfo_bytelimit "8192" // вернуть/установить значение по умолчанию
```

---

### sv_userinfo_keylimit
`cvar sv_userinfo_keylimit(int, "128")`

#### Описание и логика работы
Это максимальное количество ключей пользовательской информации, которые может создать каждый пользователь.

Значение по умолчанию — `"128"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_userinfo_keylimit "128" // вернуть/установить значение по умолчанию
```

---

### sv_writelevel
`cvar sv_writelevel(int, "35")`

#### Описание и логика работы
Указывает требуемый уровень доверия, при котором учетные записи пользователей могут записывать данные в специфичный для пользователя подкаталог /uploads/USERNAME/*. Если пусто, то загрузка не разрешена.

Значение по умолчанию — `"35"`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta sv_writelevel "35" // вернуть/установить значение по умолчанию
```

---

### vK_khr_fragment_shading_rate
`cvar vK_khr_fragment_shading_rate(string, "")`

#### Описание и логика работы
Позволяет использовать переменную скорость затенения.

Значение по умолчанию — `""`. Как и для большинства cvar движка, изменение вступает в силу немедленно, если явно не сказано иное в описании выше; для видео- и рендер-зависимых переменных иногда требуется `vid_restart` или `vid_reload`.

#### Практическое использование
```cfg
seta vK_khr_fragment_shading_rate "" // вернуть/установить значение по умолчанию
```

---

## Смежные страницы

- [Сетевые протоколы и мультиплеер](../README.md#сетевые-протоколы-и-мультиплеер)
- [Поиск серверов и мастер-серверы](../README.md#поиск-серверов-автозагрузка-контента-и-прямые-соединения)
- [Индекс справочника cvar](../README.md#переменные-движка-cvar-reference)

> [⬅ Предыдущая страница](03-audio-cvars.md) | [Следующая страница ➡](05-physics-gameplay-cvars.md)

> [⬅ Вернуться к оглавлению вики](../README.md)