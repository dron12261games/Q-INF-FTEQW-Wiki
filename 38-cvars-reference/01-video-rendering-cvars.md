# Видео, экран и общий рендеринг

> [⬅ Вернуться к оглавлению вики](../README.md)

> [Индекс справочника cvar](./README.md)

Этот раздел собирает самые важные cvar FTEQW, которые влияют на видеорежим, масштаб интерфейса, поле зрения, качество текстур, сглаживание и общий вид картинки. Список составлен по реальным объявлениям `CVAR(...)`/`CVARD(...)`/`CVARF(...)`/`CVARFD(...)` в коде движка, а описания без встроенного комментария восстановлены по контексту использования. Для параметров с `CVAR_VIDEOLATCH` безопасно считать, что изменение вступает в силу после `vid_restart`, а для `CVAR_RENDERERLATCH` — после `vid_reload` или ближайшей полной перезагрузки рендера.

## Переменные

### crosshair
`cvar crosshair(boolean/int, "1")`

#### Описание и логика работы
Включает встроенный прицел. Значение `0` полностью убирает его с экрана, а любые ненулевые значения оставляют прицел активным; в большинстве конфигураций имеет смысл использовать `0` или `1`. Переменная архивируется в конфиг (`CVAR_ARCHIVE`), поэтому подходит для постоянной пользовательской настройки и меняется на лету.

#### Практические сценарии использования
```cfg
seta crosshair "1" // обычная игра с видимым прицелом
seta crosshair "0" // скриншоты, кинематика или HUD без прицела
```

### crosshaircorrect
`cvar crosshaircorrect(boolean/int, "0")`

#### Описание и логика работы
Сдвигает прицел так, чтобы он лучше совпадал с фактической точкой попадания оружия, если модель оружия расположена ниже реальной камеры. Значение `0` оставляет классическое поведение Quake, `1` включает коррекцию; экстремально большие числа практического смысла не имеют, потому что код использует переменную как переключатель. Это semi-cheat-переменная (`CVAR_SEMICHEAT`), не архивируется автоматически и обычно применяется сразу, без рестарта.

#### Практические сценарии использования
```cfg
seta crosshaircorrect "1" // визуально честнее для оружия со смещённым muzzle point
```

### crosshairimage
`cvar crosshairimage(string, "")`

#### Описание и логика работы
Позволяет заменить стандартный прицел внешним изображением или шейдером. Пустая строка возвращает встроенный вариант, а непустое значение должно указывать на реально существующий ресурс; если путь неверный, движок обычно откатится к стандартному виду или не покажет кастомный прицел. Переменная не архивируется автоматически и меняется на лету.

#### Практические сценарии использования
```cfg
seta crosshairimage "gfx/crosshair1.tga" // использовать собственный файл прицела
```

### crosshairsize
`cvar crosshairsize(int, "8")`

#### Описание и логика работы
Задаёт виртуальный размер встроенного прицела. Малые значения делают его компактным, `0` или близкие к нулю величины фактически превращают прицел в почти незаметный, а слишком большие значения дают крупный HUD-элемент, который может мешать чтению цели. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и применяется сразу.

#### Практические сценарии использования
```cfg
seta crosshairsize "12" // увеличить прицел для высокого разрешения
seta crosshairsize "6" // сделать прицел более тонким и компактным
```

### d_lodbias
`cvar d_lodbias(float, "0")`

#### Описание и логика работы
Историческое имя cvar для смещения выбора mip-уровней текстур; в коде у неё есть алиас `gl_texture_lodbias`. Ноль оставляет штатный выбор, положительные значения заставляют рендер раньше брать более грубые mip-уровни и размывают картинку, а отрицательные делают изображение резче ценой мерцания и алиасинга на дистанции. Переменная архивируется в конфиг (`CVAR_ARCHIVE`), имеет `CVAR_RENDERERCALLBACK`, поэтому обычно применяется на лету.

#### Практические сценарии использования
```cfg
seta d_lodbias "-0.5" // сделать текстуры чуть резче
seta d_lodbias "1" // снизить shimmering на слабом железе
```

### ffov
`cvar ffov(float/string, "")`

#### Описание и логика работы
Задаёт отдельное поле зрения для нестандартных проекций, которые включаются через `r_projection`. Пустая строка означает «использовать обычный `fov`», а ненулевое число позволяет отдельно подобрать обзор для fisheye, panorama или equirectangular-режима; слишком большие величины приводят к сильным искажениям, что видно и по защитным проверкам в рендерере. Переменная не архивируется автоматически и обычно применяется сразу.

#### Практические сценарии использования
```cfg
seta ffov "180" // отдельный FOV для панорамной/рыбьеглазой проекции
```

### fov
`cvar fov(float, "90")`

#### Описание и логика работы
Основное игровое поле зрения камеры. По встроенному описанию штатный диапазон — `1-170`, стандартное значение — `90`; маленькие значения дают «зум», а слишком большие заметно искажают перспективу, особенно на узких или старых HUD-макетах. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и меняется на лету, но итоговая трактовка зависит от `scr_fov_mode`.

#### Практические сценарии использования
```cfg
seta fov "90" // классический Quake-обзор
seta fov "108" // широкий обзор в духе nQuake-конфигов
```

### gl_affinemodels
`cvar gl_affinemodels(boolean/int, "0")`

#### Описание и логика работы
Включает аффинную выборку текстур для моделей, имитируя искажения программного рендера Quake. `0` оставляет нормальную перспективно-корректную фильтрацию, `1` включает ретро-искажения; большие числа практического смысла не имеют. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и обычно применяется сразу.

#### Практические сценарии использования
```cfg
seta gl_affinemodels "1" // приблизить вид моделей к software Quake
```

### gl_compress
`cvar gl_compress(boolean/int, "0")`

#### Описание и логика работы
Разрешает автоматическое сжатие текстур драйвером даже для контента, который изначально не был сохранён в сжатом формате. `0` сохраняет максимальную предсказуемость качества, `1` экономит видеопамять и иногда ускоряет загрузку ценой возможных артефактов, особенно на UI и градиентах. Переменная архивируется в конфиг (`CVAR_ARCHIVE`); изменение лучше считать латчем уровня ресурсов и проверять после `vid_reload`.

#### Практические сценарии использования
```cfg
seta gl_compress "1" // экономить VRAM на больших HD-паках
seta gl_compress "0" // избегать артефактов компрессии на текстах и интерфейсе
```

### gl_dither
`cvar gl_dither(boolean/int, "1")`

#### Описание и логика работы
Включает или отключает аппаратный dithering там, где его поддерживает OpenGL-путь. `1` обычно помогает скрывать ступени на плавных переходах, `0` делает картинку более «чистой», но на старых путях может усилить banding; большие значения снова сводятся к логическому переключателю. Переменная не архивируется автоматически, однако код рендера отслеживает её модификацию и применяет переключение на лету.

#### Практические сценарии использования
```cfg
seta gl_dither "0" // более ровные скриншоты без зерна dithering
seta gl_dither "1" // сгладить переходы на старых путях вывода
```

### gl_finish
`cvar gl_finish(boolean/int, "0")`

#### Описание и логика работы
Принудительно вызывает `glFinish`, то есть заставляет CPU ждать полного завершения работы GPU перед продолжением кадра. `0` даёт максимальную производительность, `1` иногда помогает диагностировать микростаттер, тайминги или странности драйвера ценой ощутимого падения FPS; любые большие значения эквивалентны включению. Переменная не архивируется автоматически и действует сразу.

#### Практические сценарии использования
```cfg
seta gl_finish "1" // проверить, исчезают ли рывки из-за очереди драйвера
seta gl_finish "0" // вернуть нормальную производительность
```

### gl_lerpimages
`cvar gl_lerpimages(boolean/int, "1")`

#### Описание и логика работы
Включает более мягкий ресемплинг изображений, когда движку приходится работать с NPOT-ограничениями старых драйверов. `1` уменьшает ступенчатость и швы, `0` сохраняет более грубое классическое поведение; экстремальные значения сводятся к переключателю. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и обычно применяется без рестарта.

#### Практические сценарии использования
```cfg
seta gl_lerpimages "1" // сгладить нестандартные 2D-изображения и декали
```

### gl_max_size
`cvar gl_max_size(int, "8192")`

#### Описание и логика работы
Ограничивает максимальный размер текстуры, который движок разрешает себе использовать, даже если драйвер способен на большее. Большие значения сохраняют максимум детализации, меньшие принудительно уменьшают крупные изображения и экономят память; `0` и слишком малые числа быстро приведут к агрессивному даунскейлу и заметной потере резкости. Переменная не архивируется, имеет `CVAR_RENDERERLATCH`, поэтому безопасно менять её вместе с `vid_reload`.

#### Практические сценарии использования
```cfg
seta gl_max_size "4096" // ограничить потребление VRAM на старой видеокарте
seta gl_max_size "8192" // оставить полный размер для HD-текстур
```

### gl_motionblur
`cvar gl_motionblur(float, "0")`

#### Описание и логика работы
Управляет силой простого накопительного motion blur. В OpenGL-пути эффект выполняется только при значениях строго больше `0` и меньше `1`: `0` выключает его, числа вроде `0.2-0.6` дают заметный шлейф, а `1` и выше уже выходят за полезный диапазон и практически не дают корректного результата. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и применяется на лету.

#### Практические сценарии использования
```cfg
seta gl_motionblur "0.25" // лёгкий шлейф при движении камеры
seta gl_motionblur "0" // полностью отключить размытие движения
```

### gl_motionblurscale
`cvar gl_motionblurscale(float, "1")`

#### Описание и логика работы
Масштабирует смещение, по которому строится motion blur. Ноль почти убирает эффект даже при включённом `gl_motionblur`, `1` оставляет штатное поведение, а большие значения усиливают растягивание и быстро делают шлейф неестественным. Переменная не архивируется автоматически и применяется сразу.

#### Практические сценарии использования
```cfg
seta gl_motionblurscale "0.5" // ослабить длину шлейфа, не выключая blur полностью
```

### gl_overbright
`cvar gl_overbright(int, "1")`

#### Описание и логика работы
Управляет overbright-поведением светокарт и общей яркостью классического GL-пути. `0` даёт более тусклую и совместимую картинку, `1` — обычный компромисс, более высокие значения быстро приводят к пережогу ярких поверхностей; в старом контенте практический смысл обычно ограничен диапазоном `0-2`. Переменная архивируется в конфиг (`CVAR_ARCHIVE`), а изменение может потребовать перестройки светокарт и безопаснее проверяется после `vid_reload`.

#### Практические сценарии использования
```cfg
seta gl_overbright "0" // ближе к более тёмной классической картинке
seta gl_overbright "1" // стандартный современный режим
```

### gl_picmip
`cvar gl_picmip(int, "0")`

#### Описание и логика работы
Глобально уменьшает размер world/model-текстур по экспоненциальной схеме. `0` оставляет оригинальный размер, `1` и `2` последовательно снижают разрешение и экономят память/пропускную способность, а слишком большие числа превращают большинство материалов в очень мыльные версии. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и обычно применяется после `vid_reload` или при повторной загрузке материалов.

#### Практические сценарии использования
```cfg
seta gl_picmip "1" // умеренно снизить качество текстур ради FPS
seta gl_picmip "0" // оставить исходную детализацию
```

### gl_picmip2d
`cvar gl_picmip2d(int, "0")`

#### Описание и логика работы
Отдельно уменьшает разрешение HUD- и menu-текстур. Это полезно там, где мир хочется оставить чётким, а интерфейсный пак слишком тяжёлый; `0` ничего не режет, более высокие значения уменьшают 2D-ассеты всё сильнее. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и практичнее всего проверяется после перезагрузки графических ресурсов.

#### Практические сценарии использования
```cfg
seta gl_picmip2d "1" // облегчить тяжёлый HD-интерфейс
```

### gl_picmip_other
`cvar gl_picmip_other(int, "0")`

#### Описание и логика работы
Добавляется к `gl_picmip` именно для model-текстур. Ноль оставляет глобальное значение без надбавки, положительные числа дополнительно ухудшают только модели, что удобно для мультиплеера на слабом GPU; отрицательные значения теоретически могут компенсировать глобальный picmip, но практически обычно используют `0` и выше. Переменная архивируется в конфиг (`CVAR_ARCHIVE`).

#### Практические сценарии использования
```cfg
seta gl_picmip "1"
seta gl_picmip_other "1" // ещё сильнее упростить текстуры моделей
```

### gl_picmip_sprites
`cvar gl_picmip_sprites(int, "0")`

#### Описание и логика работы
Добавляется к `gl_picmip` только для sprite-текстур. Это позволяет отдельно делать проще дым, частицы и billboard-объекты, не ухудшая геометрию мира сильнее необходимого. Переменная архивируется в конфиг (`CVAR_ARCHIVE`); `0` значит «без дополнительного штрафа», большие значения заметно мылит спрайты.

#### Практические сценарии использования
```cfg
seta gl_picmip_sprites "1" // снизить стоимость крупных sprite-эффектов
```

### gl_picmip_world
`cvar gl_picmip_world(int, "0")`

#### Описание и логика работы
Добавляется к `gl_picmip` только для world-текстур BSP-геометрии. Ноль означает обычный глобальный режим, `1-2` дают отдельный даунскейл именно для стен, пола и архитектуры, а слишком большие значения быстро убивают читаемость окружения. Переменная архивируется в конфиг (`CVAR_ARCHIVE`).

#### Практические сценарии использования
```cfg
seta gl_picmip "0"
seta gl_picmip_world "1" // упростить только карту, оставив модели нетронутыми
```

### gl_polyblend
`cvar gl_polyblend(boolean/int, "1")`

#### Описание и логика работы
Разрешает временные полноэкранные цветовые наложения: урон, бонусы, содержимое воды и другие screen tints. `0` отключает почти все такие overlays, `1` оставляет их активными; большие числа не дают отдельного режима. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и меняется на лету; интенсивность можно дополнительно подстраивать через `gl_cshiftpercent`.

#### Практические сценарии использования
```cfg
seta gl_polyblend "0" // убрать экранные вспышки и туманы для соревновательной игры
seta gl_polyblend "1" // вернуть всю штатную визуальную обратную связь
```

### gl_smoothcrosshair
`cvar gl_smoothcrosshair(boolean/int, "1")`

#### Описание и логика работы
Сглаживает отрисовку стандартного прицела. `1` делает линии мягче и аккуратнее на высоком разрешении, `0` оставляет более жёсткий и пиксельный вид; по смыслу это обычный бинарный переключатель. Переменная не архивируется автоматически и меняется сразу.

#### Практические сценарии использования
```cfg
seta gl_smoothcrosshair "0" // подчёркнуто пиксельный олдскульный прицел
seta gl_smoothcrosshair "1" // более гладкий вид на современных мониторах
```

### gl_texture_anisotropy
`cvar gl_texture_anisotropy(int, "4")`

#### Описание и логика работы
Управляет анизотропной фильтрацией; в коде также сохранён старый алиас `gl_texture_anisotropic_filtering`. Значение `1` по сути сводит её к минимуму, `2/4/8/16` последовательно повышают качество наклонных поверхностей вроде пола и дальних стен, а слишком большие числа всё равно будут зажаты максимумом драйвера. Переменная архивируется в конфиг (`CVAR_ARCHIVE`), имеет callback и обычно подхватывается без рестарта, но работает осмысленно только вместе с линейной фильтрацией и mipmapping.

#### Практические сценарии использования
```cfg
seta gl_texture_anisotropy "16" // максимально чёткий пол и дальние плоскости
seta gl_texture_anisotropy "2" // компромисс для слабой видеокарты
```

### gl_texturemode
`cvar gl_texturemode(string, "GL_LINEAR_MIPMAP_LINEAR")`

#### Описание и логика работы
Главная переменная фильтрации world/model-текстур. Движок понимает как длинные OpenGL-имена, так и короткие трёхсимвольные схемы вроде `nll` или `lll`: первая буква управляет увеличением, вторая — выбором mipmapping, третья — уменьшением слишком крупных текстур. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и имеет callback, поэтому её можно крутить на лету; `nnn` даёт максимально «lego»-картинку, `lll` или `GL_LINEAR_MIPMAP_LINEAR` — наиболее мягкий современный вариант.

#### Практические сценарии использования
```cfg
seta gl_texturemode "nll" // пиксельное увеличение, но мягкое уменьшение вдали
seta gl_texturemode "GL_LINEAR_MIPMAP_LINEAR" // классический trilinear
```

### gl_texturemode2d
`cvar gl_texturemode2d(string, "GL_LINEAR")`

#### Описание и логика работы
То же самое, что `gl_texturemode`, но для 2D-изображений: HUD, меню, консольные панели и прочие интерфейсные текстуры. По умолчанию используется мягкая линейная фильтрация, а переход к `GL_NEAREST` или аналогичной схеме делает интерфейс жёстко пиксельным. Переменная архивируется в конфиг (`CVAR_ARCHIVE`), имеет callback и применяется на лету.

#### Практические сценарии использования
```cfg
seta gl_texturemode2d "GL_NEAREST" // резкий пиксельный HUD
seta gl_texturemode2d "GL_LINEAR" // мягкое масштабирование 2D
```

### mod_external_vis
`cvar mod_external_vis(boolean/int, "1")`

#### Описание и логика работы
Разрешает загрузку внешних `.vis`-патчей для Quake-карт, что особенно важно для корректной прозрачной воды и похожих трюков видимости. `0` отключает использование патчей и возвращает поведение ближе к исходному BSP, `1` разрешает расширенную VIS-информацию; большие значения смысла не имеют. Переменная не архивируется автоматически, а эффект обычно проявляется при следующей загрузке карты.

#### Практические сценарии использования
```cfg
seta mod_external_vis "1" // дать картам с vis-патчами корректную прозрачную воду
```

### r_drawflat
`cvar r_drawflat(boolean/int, "0")`

#### Описание и логика работы
Показывает мир в режиме плоских заливок вместо нормальных текстур; в коде есть алиас `gl_textureless`. Значение `0` рисует материалы как обычно, `1` включает flat-shading; это semi-cheat-переменная (`CVAR_SEMICHEAT`), полезная для анализа геометрии, читаемости силуэтов и поиска лишнего визуального шума. Переменная архивируется в конфиг (`CVAR_ARCHIVE`), использует renderer callback и меняется быстро, но на сервере с жёсткими правилами может быть ограничена.

#### Практические сценарии использования
```cfg
seta r_drawflat "1" // проверить читаемость уровня без текстурного шума
seta r_drawflat "0" // вернуть нормальную картинку
```

### r_drawviewmodel
`cvar r_drawviewmodel(boolean/int, "1")`

#### Описание и логика работы
Управляет отрисовкой оружия и прочей first-person модели игрока. `0` полностью убирает viewmodel, `1` оставляет её штатно включённой; экстремальные значения снова сводятся к обычному переключателю. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и применяется на лету.

#### Практические сценарии использования
```cfg
seta r_drawviewmodel "0" // освободить центр экрана для соревновательной игры
seta r_drawviewmodel "1" // классический вид от первого лица
```

### r_fxaa
`cvar r_fxaa(boolean/int, "0")`

#### Описание и логика работы
Включает постпроцессный FXAA, который сглаживает лесенки уже по готовому кадру. `0` отключает фильтр, `1` включает его; это дешёвый способ немного успокоить aliasing, но он неизбежно слегка размывает мелкие детали и HUD-переходы. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и обычно применяется на лету в постпроцесс-пайплайне.

#### Практические сценарии использования
```cfg
seta r_fxaa "1" // быстрое сглаживание без MSAA
seta r_fxaa "0" // максимально резкая картинка
```

### r_lodbias
`cvar r_lodbias(int, "0")`

#### Описание и логика работы
Смещает выбор model-LOD для тех моделей, у которых вообще есть уровни детализации. Ноль оставляет штатный порог, положительные значения заставляют раньше переходить на более грубые версии, а отрицательные дольше удерживают высокодетализированные модели на экране. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и обычно применяется на лету.

#### Практические сценарии использования
```cfg
seta r_lodbias "1" // раньше переключаться на упрощённые модели
seta r_lodbias "-1" // удерживать детальные модели подольше
```

### r_lodscale
`cvar r_lodscale(float, "5")`

#### Описание и логика работы
Масштабирует агрессивность model-LOD. Большие значения усиливают сокращение детализации на расстоянии, маленькие ослабляют его; значение `0` или почти `0` стремится удерживать более детальные LOD как можно дольше и может увеличить нагрузку. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и действует без полного рестарта.

#### Практические сценарии использования
```cfg
seta r_lodscale "8" // сильнее экономить на дальних моделях
seta r_lodscale "2" // сохранить больше детализации вблизи и на средней дистанции
```

### r_noframegrouplerp
`cvar r_noframegrouplerp(boolean/int, "0")`

#### Описание и логика работы
Отключает интерполяцию между кадрами групп анимации у классических моделей. `0` оставляет плавный переход, `1` возвращает более ступенчатый и аутентичный старый вид; полезно для любителей точной Quake-эстетики или при работе с проблемным контентом. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и меняется на лету.

#### Практические сценарии использования
```cfg
seta r_noframegrouplerp "1" // максимально жёсткая классическая анимация mdl
```

### r_nolerp
`cvar r_nolerp(boolean/int, "0")`

#### Описание и логика работы
Глобально отключает интерполяцию моделей между кадрами и позициями там, где она доступна. `0` даёт современную плавность, `1` возвращает рывковую старую подачу; для демо- и ретро-конфигов это бывает желанно, но в обычной игре делает анимацию ощутимо менее мягкой. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и применяется сразу.

#### Практические сценарии использования
```cfg
seta r_nolerp "1" // ретро-подача и подчёркнутая дискретность моделей
seta r_nolerp "0" // плавная современная интерполяция
```

### r_projection
`cvar r_projection(int, "0")`

#### Описание и логика работы
Выбирает тип геометрической проекции. По встроенному описанию: `0` — обычная перспектива, `1` — stereographic, `2` — fisheye, `3` — panoramic, `4` — Lambert azimuthal equal-area, `5` — equirectangular, `6` — panini; чем экзотичнее режим, тем выше риск, что HUD, скриншоты и сторонние моды будут выглядеть непривычно. Переменная не архивируется автоматически и применяется сразу, а для комфортной картинки в нестандартных режимах часто нужен отдельный `ffov`.

#### Практические сценарии использования
```cfg
seta r_projection "0" // обычный игровой режим
seta r_projection "5" // equirectangular для панорамного захвата
```

### r_renderscale
`cvar r_renderscale(float, "1")`

#### Описание и логика работы
Заставляет движок рендерить 3D-сцену во внутреннем разрешении, отличном от итогового. `1` — нативный масштаб, числа меньше `1` включают subsampling и ускоряют отрисовку ценой мыла, числа больше `1` дают supersampling с лучшей чёткостью, а отрицательные значения полезны для нарочито пиксельного апскейла — по крайней мере Vulkan-путь явно переключает такой режим на nearest. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и обычно применяется без `vid_restart`, хотя кратковременная пересборка рендер-таргетов возможна.

#### Практические сценарии использования
```cfg
seta r_renderscale "0.75" // выиграть FPS за счёт внутреннего downscale
seta r_renderscale "1.5" // улучшить антиалиасинг и резкость supersampling'ом
```

### r_showtris
`cvar r_showtris(boolean/int, "0")`

#### Описание и логика работы
Рисует геометрию каркасом поверх обычной сцены; в коде это та же переменная, что привычный алиас `r_wireframe`. `0` отключает режим, `1` включает; это cheat-переменная (`CVAR_CHEAT`), так что на обычном сервере её могут не разрешить. Автоматически не архивируется и применяется сразу.

#### Практические сценарии использования
```cfg
seta r_showtris "1" // проверить плотность геометрии и silhouette readability
seta r_showtris "0" // вернуть нормальный рендер
```

### r_viewmodel_fov
`cvar r_viewmodel_fov(float/string, "")`

#### Описание и логика работы
Позволяет задать отдельный FOV для модели оружия. Пустая строка означает «использовать тот же базовый FOV/автонастройку», а явное число помогает уменьшить рыбий глаз на пушке при широком `fov` или, наоборот, сделать оружие более крупным. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и применяется на лету.

#### Практические сценарии использования
```cfg
seta r_viewmodel_fov "90" // нормализовать размер оружия при широком игровом FOV
seta r_viewmodel_fov "" // вернуть автоматическое поведение
```

### r_viewmodel_quake
`cvar r_viewmodel_quake(boolean/int, "0")`

#### Описание и логика работы
Возвращает «странные» движения viewmodel из оригинального Quake. `0` оставляет более аккуратную современную подачу, `1` включает историческую качку и смещения; это чисто вкусовой параметр отображения от первого лица. Переменная не архивируется автоматически и меняется сразу.

#### Практические сценарии использования
```cfg
seta r_viewmodel_quake "1" // ближе к ванильной подаче оружия
```

### r_wateralpha
`cvar r_wateralpha(float, "1")`

#### Описание и логика работы
Управляет прозрачностью воды. `1` означает полностью непрозрачную классическую поверхность, значения меньше `1` делают воду прозрачнее, а `0` максимально открывает обзор сквозь неё; отрицательные числа и слишком большие значения обычно не дают полезного художественного результата. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и работает в рамках shader system, так что визуальный эффект виден сразу или после небольшого обновления материала.

#### Практические сценарии использования
```cfg
seta r_wateralpha "0.5" // полупрозрачная вода для лучшей читаемости подводных зон
seta r_wateralpha "1" // классическая непрозрачная вода
```

### r_waterstyle
`cvar r_waterstyle(int, "1")`

#### Описание и логика работы
Выбирает способ отрисовки воды и телепортеров. Встроенные режимы такие: `0` — fastturb-цвет, `1` — обычная q1-вода, `2` — преломление, `3` — преломление с отражением, `4` — ripplemap без отражений, `5` — ripplemap с отражениями; чем выше режим, тем выше требования к шейдерам, fillrate и буферам. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и относится к shader system.

#### Практические сценарии использования
```cfg
seta r_waterstyle "1" // недорогая классическая вода
seta r_waterstyle "3" // преломление и отражение для более богатой картинки
```

### r_waterwarp
`cvar r_waterwarp(float, "1")`

#### Описание и логика работы
Включает полноэкранный warp-эффект под водой; встроенное описание отдельно отмечает, что `-1` принудительно заставляет использовать старый FOV-warp fallback. `0` полностью убирает искажение, `1` оставляет штатный эффект, положительные значения больше единицы делают его сильнее, а `-1` полезен для диагностики или слабых систем. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и применяется на лету.

#### Практические сценарии использования
```cfg
seta r_waterwarp "0" // убрать подводное искажение ради читаемости
seta r_waterwarp "-1" // принудить старый FOV-warp fallback
```

### scr_fov_mode
`cvar scr_fov_mode(int, "4")`

#### Описание и логика работы
Определяет, как именно трактуется `fov` при разных соотношениях сторон. По описанию из кода: `0` — по большей оси, `1` — по меньшей, `2` — строго горизонтальный FOV, `3` — строго вертикальный, `4` — горизонтальный FOV для 4:3 с паддингом на широких экранах; именно `4` лучше всего сохраняет «классическое» ощущение на widescreen. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и меняется сразу.

#### Практические сценарии использования
```cfg
seta scr_fov_mode "4" // классическое поведение на широком мониторе
seta scr_fov_mode "2" // жёстко контролировать горизонтальный угол обзора
```

### vid_bpp
`cvar vid_bpp(int, "0")`

#### Описание и логика работы
Задаёт желаемую цветовую глубину видеорежима. `0` означает «не навязывать, пусть движок/драйвер решат сами», типичные ручные значения — `24` или `32`; слишком низкие режимы на современных Windows всё равно могут быть подняты движком до совместимого уровня. Переменная архивируется в конфиг (`CVAR_ARCHIVE`), имеет `CVAR_VIDEOLATCH` и требует `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_bpp "32" // явно запросить стандартный современный цветовой режим
```

### vid_conautoscale
`cvar vid_conautoscale(float, "0")`

#### Описание и логика работы
Масштабирует весь 2D-слой: HUD, консоль, шрифты и меню. Ноль оставляет авто/стандартное поведение для desktop-сборки, значения вроде `1.25`, `1.5` и `2` увеличивают интерфейс, а слишком высокие будут зажаты так, чтобы сохранить хотя бы виртуальные `320x200`; в Android-ветке у этой же переменной другой дефолт, но desktop-код задаёт `0`. Переменная архивируется (`CVAR_ARCHIVE`), имеет renderer callback и применяется на лету.

#### Практические сценарии использования
```cfg
seta vid_conautoscale "1.5" // крупнее консоль и HUD на 1440p/4K
seta vid_conautoscale "0" // позволить движку выбрать штатный масштаб
```

### vid_conheight
`cvar vid_conheight(int, "0")`

#### Описание и логика работы
Принудительно задаёт виртуальную высоту экрана для 2D-слоя. `0` значит «вычислить автоматически на основе ширины, аспекта и других настроек», а явные значения полезны модам, которые хотят фиксированную виртуальную сетку; слишком маленькие числа сделают интерфейс огромным и грубым, слишком большие — очень мелким. Переменная архивируется в конфиг (`CVAR_ARCHIVE`) и обычно начинает работать сразу.

#### Практические сценарии использования
```cfg
seta vid_conheight "240" // виртуальная высота в стиле старых low-res HUD
```

### vid_conwidth
`cvar vid_conwidth(int, "0")`

#### Описание и логика работы
Принудительно задаёт виртуальную ширину экрана для HUD и консоли. Разработчик прямо рекомендует обычно оставлять `0`, чтобы движок сам учитывал аспект и видеорежим; ручные значения полезны только при жёстко спроектированном интерфейсе. Переменная архивируется (`CVAR_ARCHIVE`), имеет renderer callback и меняется на лету.

#### Практические сценарии использования
```cfg
seta vid_conwidth "640" // зафиксировать виртуальную ширину для кастомного HUD
seta vid_conwidth "0" // вернуть автоматический выбор
```

### vid_depthbits
`cvar vid_depthbits(int, "0")`

#### Описание и логика работы
Задаёт желаемую глубину Z-буфера. `0` означает автоматический выбор, `24` — обычный современный запрос, меньшие значения могут усиливать z-fighting, а слишком завышенные просто будут проигнорированы, если backend их не поддерживает. Переменная архивируется (`CVAR_ARCHIVE`), имеет `CVAR_VIDEOLATCH` и требует `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_depthbits "24" // хороший практический выбор для большинства карт
```

### vid_desktopsettings
`cvar vid_desktopsettings(boolean/int, "0")`

#### Описание и логика работы
Заставляет движок игнорировать `vid_width` и `vid_height` и использовать параметры рабочего стола. `0` даёт обычный ручной контроль разрешения, `1` делает fullscreen/windowed-fullscreen максимально дружелюбным к текущему десктопу; это особенно удобно для быстрой смены backend'ов и мониторных конфигураций. Переменная архивируется (`CVAR_ARCHIVE`), имеет `CVAR_VIDEOLATCH` и требует `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_desktopsettings "1" // всегда стартовать в текущем разрешении рабочего стола
```

### vid_displayfrequency
`cvar vid_displayfrequency(int, "0")`

#### Описание и логика работы
Историческое имя cvar, которое управляет желаемой частотой обновления экрана; в коде оно хранится в переменной `vid_refreshrate`. `0` обычно означает «не запрашивать конкретную герцовку», а явные значения вроде `60`, `120` или `144` полезны только там, где драйвер и режим действительно поддерживают их. Переменная архивируется (`CVAR_ARCHIVE`), имеет `CVAR_VIDEOLATCH` и требует `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_displayfrequency "144" // попробовать запросить 144 Гц в эксклюзивном fullscreen
```

### vid_fullscreen
`cvar vid_fullscreen(int, "2")`

#### Описание и логика работы
Определяет режим полноэкранного вывода. На обычных desktop-сборках `0` — обычное изменяемое окно, `1` — настоящий fullscreen exclusive, `2` — borderless/maximized fullscreen; именно `2` выбран значением по умолчанию, потому что чаще всего лучше дружит с современными ОС. Переменная архивируется (`CVAR_ARCHIVE`), помечена `CVAR_VIDEOLATCH`, а описание из кода прямо говорит о необходимости `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_fullscreen "2" // современный borderless fullscreen
seta vid_fullscreen "1" // эксклюзивный fullscreen для старых драйверов или VRR-тестов
```

### vid_hardwaregamma
`cvar vid_hardwaregamma(int, "1")`

#### Описание и логика работы
Управляет тем, как движок применяет гамму и контраст: через аппаратные gamma ramps, GLSL или комбинированный путь. По описанию из кода: `0` — «уродливая» текстурная гамма, `1` — GLSL в окне / hardware в fullscreen, `2` — всегда GLSL, `3` — всегда hardware gamma, `4` — только гамма для сцены; неподдерживаемые режимы могут быть частично проигнорированы драйвером. Переменная архивируется (`CVAR_ARCHIVE`), имеет `CVAR_RENDERERLATCH`, поэтому безопаснее проверять её после `vid_reload`.

#### Практические сценарии использования
```cfg
seta vid_hardwaregamma "1" // обычный рекомендованный режим
seta vid_hardwaregamma "2" // принудить shader-based gamma в окне
```

### vid_height
`cvar vid_height(int, "0")`

#### Описание и логика работы
Запрашиваемая физическая высота окна или эксклюзивного режима в пикселях. `0` означает «использовать разрешение рабочего стола», а любые положительные значения имеют смысл только вместе с подходящим `vid_fullscreen`/`vid_width`; слишком маленькие числа дают заведомо низкое внутреннее разрешение и крупный апскейл. Переменная архивируется (`CVAR_ARCHIVE`), имеет `CVAR_VIDEOLATCH` и требует `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_height "1080" // запросить режим 1920x1080 вместе с vid_width
seta vid_height "0" // всегда брать высоту рабочего стола
```

### vid_multisample
`cvar vid_multisample(int, "0")`

#### Описание и логика работы
Запрашивает MSAA при создании видеоконтекста; у переменной также есть алиас `vid_samples`. По встроенному описанию `1` явно выключает MSAA, `0` оставляет выбор движку/драйверу, а `2`, `4`, `8` и выше пытаются запросить соответствующее число сэмплов; если устройство не умеет столько, backend зажмёт значение до доступного режима. Переменная архивируется (`CVAR_ARCHIVE`) и на практике требует пересоздания swapchain/контекста, поэтому надёжнее выполнять `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_multisample "4" // 4x MSAA
seta vid_multisample "1" // явно отключить MSAA
```

### vid_renderer
`cvar vid_renderer(string, "")`

#### Описание и логика работы
Выбирает backend рендера. Пустая строка позволяет движку решить самому, а явные значения вроде `vk`, `gl`, `egl`, `d3d9`, `d3d11`, `d3d11 warp`, `headless` или `sv` принуждают конкретный путь; если строка неверная или backend недоступен, движок попытается откатиться к рабочему варианту. Переменная архивируется (`CVAR_ARCHIVE`), имеет `CVAR_VIDEOLATCH` и требует `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_renderer "vk" // запускать игру через Vulkan
seta vid_renderer "gl" // вернуться на OpenGL-путь
```

### vid_srgb
`cvar vid_srgb(int, "0")`

#### Описание и логика работы
Управляет работой с sRGB/линейным цветовыми пространствами. По встроенному описанию `-1` переводит только framebuffer в sRGB-логику, `0` полностью выключает режим и может усиливать banding, `1` включает нормальную sRGB-коррекцию, `2` пытается использовать линейный floating-point framebuffer для HDR-подобного вывода; поведение драйверов здесь исторически разнится очень сильно. Переменная архивируется (`CVAR_ARCHIVE`); часть backend'ов умеет подхватывать её мягко, но надёжнее тестировать после `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_srgb "1" // корректный sRGB-пайплайн для современного освещения
seta vid_srgb "0" // максимальная совместимость со старыми путями
```

### vid_vsync
`cvar vid_vsync(int, "0")`

#### Описание и логика работы
Управляет ожиданием вертикальной синхронизации; в коде у переменной есть алиас `vid_wait`. Значение `0` отключает VSync, `1` ждёт один vblank, `2` фактически ограничивает вывод каждым вторым обновлением, а пустая строка или отрицательное число переводят поведение в режим «backend/driver decide»; в OpenGL на Windows изменение подхватывается callback'ом сразу, а в Vulkan приводит к пересозданию swapchain. Переменная архивируется в конфиг (`CVAR_ARCHIVE`).

#### Практические сценарии использования
```cfg
seta vid_vsync "1" // убрать tearing ценой дополнительной задержки
seta vid_vsync "0" // минимальная задержка ввода и максимум FPS
```

### vid_width
`cvar vid_width(int, "0")`

#### Описание и логика работы
Запрашиваемая физическая ширина окна или полноэкранного режима в пикселях. `0` означает использование ширины рабочего стола, а явные значения вроде `1280`, `1920` или `2560` полезны при ручном выборе режима; слишком маленькие числа дают грубый апскейл и ухудшают читаемость интерфейса, если отдельно не настроен виртуальный 2D-размер. Переменная архивируется (`CVAR_ARCHIVE`), имеет `CVAR_VIDEOLATCH` и требует `vid_restart`.

#### Практические сценарии использования
```cfg
seta vid_width "1920" // запросить Full HD вместе с vid_height 1080
seta vid_width "0" // всегда брать ширину рабочего стола
```

## Смежные страницы

- [Настраиваемые переменные движка (cvar)](../19-config-console/cvars-engine-variables.md)
- [Переключение backend'ов рендера](../15-renderer-backends/renderer-backend-switch.md)
- [Индекс справочника cvar](./README.md)
