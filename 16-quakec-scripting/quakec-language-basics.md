﻿# Синтаксис языка QuakeC: основы

> [⬅ Вернуться к оглавлению вики](../README.md)

> Раздел: [Игровая логика: язык QuakeC](./README.md)

## Что это даёт геймдизайнеру

Прежде чем пользоваться справочником встроенных функций и стандартными полями объектов, стоит один раз разобраться с самим языком, на котором пишется вся игровая логика — **QuakeC**. Это не язык общего назначения вроде C++ или Python: он был создан специально для описания игровой логики Quake ещё в 1996 году и намеренно устроен просто — без указателей, без сложных структур данных, зато с очень прямой связью между кодом и тем, что происходит в игре. Разобравшись один раз с базовым синтаксисом, вы сможете читать и писать любой код на SSQC, CSQC или MenuQC — все три используют один и тот же язык, различаясь лишь набором доступных функций и тем, на чьей стороне (сервер/клиент/меню) выполняется код.

Важное уточнение сразу: то, что описано на этой странице, — это возможности **компилятора FTEQCC** (см. [«Компилятор FTEQCC»](./fteqcc-compiler.md)), который используется для сборки логики в движке FTEQW. Самый первый, оригинальный QuakeC от id Software 1996 года был заметно беднее — в нём, например, не было циклов `for`, оператора `switch`, тернарного оператора `?:` и многих других удобств, перечисленных ниже. Там, где это важно, мы отдельно отмечаем: «это было и в оригинальном QuakeC» или «это добавил FTEQCC».

## Типы данных

В QuakeC всего несколько базовых типов данных — и это фундаментальное отличие от большинства современных языков программирования:

| Тип | Что хранит | Пример |
|---|---|---|
| `float` | Число с плавающей точкой (используется и для чисел, и для «условной правды/лжи» — 0 считается ложью, любое другое число правдой) | `float health;` |
| `vector` | Три числа сразу — точка в пространстве или направление (x, y, z) | `vector origin;` |
| `string` | Текстовая строка | `string message;` |
| `entity` | Ссылка на игровой объект (сущность) в мире | `entity target;` |
| `void` | «Пустота» — используется только как тип возвращаемого значения функции, которая ничего не возвращает | `void() Explode;` |

Сверх этих исходных пяти типов FTEQCC добавляет ряд дополнительных типов данных для более сложных задач — они рассмотрены отдельно на странице [«Расширенные типы данных в QuakeC»](./extended-quakec-datatypes.md) (например, целые числа `int`, беззнаковые `unsigned`/`unsigned long`, ссылки на функции с сигнатурой и другие).

## Переменные и поля объектов

QuakeC различает **обычные переменные** (глобальные или локальные) и **поля объектов** (entity fields) — то, что хранится не «само по себе», а прикреплено к конкретному игровому объекту, как ячейка в его личной анкете.

```c
float   myglobalcounter;   // обычная глобальная переменная — одна на весь мир игры

.float  health;            // ПОЛЕ объекта — у КАЖДОГО объекта своё собственное значение health
.vector origin;             // поле-вектор — тоже у каждого объекта своё
```

Разница видна по точке перед именем: `.float health;` объявляет поле, которое есть у любого объекта в игре (и `self.health`, и `other.health`, и `some_monster.health` — у каждого своё собственное число). Без точки — `float myglobalcounter;` — это одна-единственная переменная на всю игру, общая для всех.

Обращение к полю конкретного объекта пишется через точку после имени объекта:

```c
self.health = self.health - 10;   // отнять 10 очков здоровья у текущего объекта
other.frags = other.frags + 1;    // добавить одно очко второму участнику события
```

## Функции

Функция в QuakeC объявляется необычным для многих других языков образом — тип возвращаемого значения указывается вместе со списком типов аргументов в скобках, а имя функции идёт уже после этого блока:

```c
// Функция без аргументов, ничего не возвращает
void() SUB_Null =
{
	// тело функции
};

// Функция с двумя аргументами (entity и float), возвращает float
float(entity who, float amount) TakeDamage =
{
	who.health = who.health - amount;
	return who.health <= 0;
};
```

Такая форма — `тип_результата(тип_аргумента1, тип_аргумента2, ...) ИмяФункции` — используется и для объявления обычных функций с телом на QuakeC, и (без блока `{ }`, а со специальным значением вместо него) для объявления **встроенных функций движка** — тех самых builtins из справочника:

```c
void(entity e, string s) sprint = #24;   // это builtin: движок сам знает, что делает функция #24
```

Число после `#` — это номер встроенной функции внутри движка; такие функции не имеют тела на QuakeC, их поведение целиком реализовано внутри движка на низком уровне (подробнее — во всех статьях [справочника встроенных функций](../37-quakec-builtins-reference/README.md)).

Ссылку на функцию можно хранить и передавать как обычное значение — например, в поле `think`:

```c
self.think = SUB_Null;
self.nextthink = time + 1;
```

## Операторы

Большинство операторов знакомо по другим языкам программирования: `+`, `-`, `*`, `/`, `==`, `!=`, `<`, `>`, `<=`, `>=`, `&&` (логическое И), `||` (логическое ИЛИ), `!` (логическое НЕ), `&` и `|` (побитовые операции).

FTEQCC поверх исходного языка добавляет дополнительные, привычные по языку C операторы, которых не было в оригинальном QuakeC 1996 года: составное присваивание `+=`, `-=`, `*=`, `/=`, инкремент/декремент `++`/`--`, а также тернарный (условный) оператор `?:`:

```c
score += 10;                       // то же самое, что score = score + 10;
frags++;                           // то же самое, что frags = frags + 1;
float bonus = (won) ? 100 : 0;     // если won истинно — 100, иначе — 0
```

## Управляющие конструкции

Ветвление `if`/`else` было в языке всегда:

```c
if (self.health <= 0)
{
	self.think = Die;
}
else
{
	self.think = Idle;
}
```

Циклы `while` и `do ... while` также присутствовали в оригинальном QuakeC. А вот классический **цикл `for`** и оператор множественного выбора **`switch`/`case`** — это уже расширения, добавленные FTEQCC (в самом первом QuakeC их эмулировали через `while` с ручными счётчиками, что было заметно неудобнее):

```c
// цикл for — расширение FTEQCC
for (float i = 0; i < 10; i++)
{
	dprint(ftos(i));
}

// switch/case — расширение FTEQCC
switch (self.weapon)
{
case 1:
	self.currentammo = self.ammo_shells;
	break;
case 2:
	self.currentammo = self.ammo_nails;
	break;
default:
	self.currentammo = 0;
}
```

Также доступны `break` (досрочный выход из цикла/switch) и `continue` (переход к следующей итерации цикла), а также редко используемый `goto` с именованными метками — для прямых, безусловных переходов внутри функции.

## Комментарии

Поддерживаются оба знакомых по языку C стиля комментариев:

```c
// однострочный комментарий — до конца строки

/*
   многострочный комментарий —
   может занимать сколько угодно строк
*/
```

## Локальные переменные

Внутри тела функции переменные объявляются ключевым словом `local` (в современном FTEQCC также разрешено объявлять переменную прямо там, где она нужна, без обязательного вынесения в начало функции, и даже без слова `local` вовсе, если сразу указан тип):

```c
void() Example =
{
	local float count;
	count = 5;

	// в современном FTEQCC можно и так, с сразу заданным значением:
	float total = 0;
};
```

## Точка с запятой и блоки кода

Каждое отдельное выражение или объявление в QuakeC завершается точкой с запятой `;` — точно как в языке C. Блок из нескольких инструкций обрамляется фигурными скобками `{ }`. Пропущенная точка с запятой — самая частая причина ошибки компиляции у новичков.

## Кадровые макросы (`$frame`) — анимация «по-старому»

В классических (не скелетных) моделях Quake анимация задаётся не именем, а простым номером кадра модели, хранящимся в поле `.frame`. Чтобы не считать эти номера в уме, файлы моделей (`.mdl`) сопровождаются специальным заголовком с частично-декларативным синтаксисом, который вставляется прямо в исходный код QuakeC через специальный символ `$`:

```c
$frame walk1 walk2 walk3 walk4 walk5 walk6 walk7 walk8

void() walk1 =
{
	self.frame = $walk1;    // $walk1 превратится в порядковый номер кадра при компиляции
	self.nextthink = time + 0.1;
	self.think = walk2;
};
```

Директива `$frame` перечисляет имена кадров подряд, начиная с текущего счётчика (обычно 0), и каждому присваивает следующий номер по порядку. Затем в коде можно использовать `$имя_кадра` вместо того, чтобы помнить конкретное число — компилятор сам подставит нужное целое значение. Похожие директивы — `$modelname` (указывает, к какой модели относятся следующие `$frame`), `$cd`, `$origin`, `$base`, `$scale`, `$skin` (используются инструментами создания моделей, самим компилятором игнорируются) и `$framevalue`/`$framerestore` (вручную задают текущий счётчик кадров). Этот механизм — наследие самого первого Quake и до сих пор широко используется в классических (не скелетных) монстрах и оружии.

## Смежные страницы

- [Компилятор FTEQCC](./fteqcc-compiler.md)
- [Стандартные заголовочные файлы для QuakeC](./standard-header-files.md)
- [Расширенные типы данных в QuakeC](./extended-quakec-datatypes.md)
- [Серверная игровая логика (SSQC)](./server-side-quakec-ssqc.md)
- [Справочник встроенных функций QuakeC](../37-quakec-builtins-reference/README.md)
