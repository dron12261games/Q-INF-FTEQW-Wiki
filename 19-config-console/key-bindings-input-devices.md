# Привязка клавиш и устройств ввода

> [⬅ Предыдущая страница](startup-parameters.md) | [Следующая страница ➡](../20-player-userinfo/player-userinfo-name-skin-team.md)

> [⬅ Вернуться к оглавлению вики](../README.md)

## Что это даёт геймдизайнеру

Система привязок позволяет назначить любое игровое действие (движение, стрельбу, открытие меню, произвольную команду или алиас) на любую клавишу клавиатуры, кнопку мыши, джойстика, геймпада или даже кнопку VR-контроллера. Это касается как игрока, настраивающего управление под себя, так и мода, который может поставляться с разумной раскладкой «по умолчанию».

---

## Интерфейс настройки

Основные команды:

| Команда | Назначение |
|---|---|
| [`bind <key> <command>`](../44-cli-commands-reference/02-client-ui-commands.md#bind) | привязать команду к клавише, например [`bind MOUSE1 +attack`](../44-cli-commands-reference/02-client-ui-commands.md#bind) |
| [`unbind <key>`](../44-cli-commands-reference/02-client-ui-commands.md#unbind) | снять привязку с клавиши |
| [`unbindall`](../44-cli-commands-reference/02-client-ui-commands.md#unbindall) | снять вообще все привязки клавиш (используется осторожно, обычно только в собственном `default.cfg` мода перед заданием новой раскладки «с чистого листа») |

Можно привязывать и комбинации с модификаторами — `ctrl`, `shift`, `alt`: например, [`bind ctrl+shift+alt+k kill`](../44-cli-commands-reference/02-client-ui-commands.md#bind) назначит команду только на нажатие клавиши `K` при одновременно зажатых всех трёх модификаторах.

### Привязки для конкретного устройства ввода и уровень доступа

Команда **[`in_bind <number_device> <key> <command>`](../44-cli-commands-reference/02-client-ui-commands.md#in_bind)** — расширенный вариант [`bind`](../44-cli-commands-reference/02-client-ui-commands.md#bind), привязывающий команду не глобально, а к конкретному подключённому устройству ввода (например, к конкретной клавиатуре или геймпаду в системах с несколькими одновременно подключёнными игроками за одним компьютером, «split-screen»/hot-seat режим) — благодаря этому у каждого локального игрока может быть собственная независимая раскладка клавиш даже при использовании одинаковых физических клавиш на разных устройствах.

Команда **[`bindlevel <key> <level_access> <command>`](../44-cli-commands-reference/02-client-ui-commands.md#bindlevel)** — привязывает команду к клавише с явным указанием уровня прав выполнения (по аналогии с [`rcon`](../44-cli-commands-reference/02-client-ui-commands.md#rcon)/уровнями доступа консольных команд), что позволяет ограничить, какие именно клавиши могут выполнять потенциально «нечестные» команды при выполнении конфигов, присланных с недоверенного источника (например, сервером).

Для действий с двумя состояниями (нажатие/отпускание — например, движение вперёд или стрельба) используется пара команд с префиксами `+`/`-`:

```
bind "w" "+forward"
```

Здесь при нажатии клавиши будет вызвана команда [`+forward`](../44-cli-commands-reference/02-client-ui-commands.md#forward), а при отпускании — автоматически [`-forward`](../44-cli-commands-reference/02-client-ui-commands.md#forward). Это стандартный способ описать «удержание» кнопки, а не разовое нажатие.

**Джойстики и геймпады.** Оси устройств (стики, триггеры) настраиваются через отдельный набор переменных вида `joy_advaxis`, `joy_anglesens`, `joy_movesens`, `joy_anglethreshold`, `joy_movethreshold` — они определяют, какому игровому действию соответствует каждая ось (движение вперёд/назад, поворот камеры и т.д.), а также чувствительность каждой оси. Переменная [`joyexponent`](../38-cvars-reference/06-ui-console-input-cvars.md#joyexponent) делает отклик стика нелинейным — для большей точности прицеливания около центра стика, а [`joyradialdeadzone`](../38-cvars-reference/07-system-misc-cvars.md#joyradialdeadzone) управляет «мёртвой зоной» (минимальным отклонением стика, которое ещё не считается движением): можно обрабатывать оси по отдельности (квадратная зона), совместно (круглая, более естественная зона) или вовсе доверить обработку мёртвых зон операционной системе.

**VR-контроллеры.** Для VR-режима положения и состояния кнопок контроллеров доступны отдельно и могут использоваться как источник ввода, в том числе игровой логикой мода напрямую (см. раздел [«VR и стереоскопия»](../README.md#экран-vr-и-выбор-рендерера)).

---

## Примеры

- Простая привязка выхода из игры на клавишу: [`bind "F12" "quit"`](../44-cli-commands-reference/02-client-ui-commands.md#bind).
- Полная переработанная раскладка управления для леворукого игрока, сохранённая в отдельном конфиге `lefty.cfg` и подключаемая командой [`exec lefty.cfg`](../44-cli-commands-reference/01-fteqw-startup-parameters.md#exec).
- Мод переопределяет управление, вызывая [`unbindall`](../44-cli-commands-reference/02-client-ui-commands.md#unbindall), а затем задавая собственный набор привязок «с нуля» в своём стартовом конфиге.

---

## Смежные страницы

- [Алиасы и командные макросы](./aliases-macros.md)
- [VR-ввод (контроллеры, отслеживание рук)](../14-vr-stereo/vr-input-hand-tracking.md)

> [⬅ Предыдущая страница](startup-parameters.md) | [Следующая страница ➡](../20-player-userinfo/player-userinfo-name-skin-team.md)

> [⬅ Вернуться к оглавлению вики](../README.md)